export const content: Record<string, string> = {
  "how-triggers-work": `
## トリガーの仕組み

トリガーはエージェントの「いつ」です。プロンプトとツールはエージェントが *何をする* かを定義し、トリガーは *いつ、どんな入力で* を定義します。Personas は 7 つのトリガータイプを搭載しています: **manual**(ボタンクリック)、**schedule**(cron 形式)、**webhook**(インバウンド HTTP)、**clipboard**(コピーイベントのマッチ)、**file watcher**(ファイルシステムイベント)、**chain**(別のエージェントの出力)、**event-based**(他のエージェント、プラグイン、エンジン自体が発行する内部イベント)。

各エージェントは、タイプを混在させた任意の数のトリガーを持つことができます。1 つのエージェントが、日次スケジュールで実行され、Stripe からの webhook に反応し、メールアドレスをコピーしたときに発火し、上流エージェントからチェーン可能 — すべて同時に。

### トリガータイプ

:::compare
**Manual**
エディターでのボタンクリック、またはタイトルバーのクイック実行から。すべてのエージェントがデフォルトでこれを取得します。テストとアドホック呼び出しに最適。
---
**Schedule**
cron ベース。時次、日次、週次、または完全な cron 式とタイムゾーン。入力なしで実行されるルーチン作業 — 日次サマリー、週次レポートに最適。
---
**Webhook**
エージェントがリッスンする一意のインバウンド URL。外部サービスが POST してエージェントを開始します。「サードパーティサービスのイベントに反応する」に最適。
---
**Clipboard**
コピーされたテキストが構成されたパターン(regex、コンテンツタイプ、またはキーワード)に一致すると発火します。パワーユーザーショートカットに最適 — メールをコピーすると、エージェントがそれを調べる。
---
**File Watcher**
監視されたフォルダ上のファイルシステムイベント(作成 / 変更 / 削除)。ファイルが予測不可能な時間に到着するドロップゾーンワークフローに最適。
---
**Chain**
エージェント A の出力がエージェント B の入力になります。焦点を絞ったエージェントで構成された多段パイプラインに最適。
---
**Event-Based**
内部 Personas イベント(認証情報失効、プラグインがイベントを発行、manual_review で実行が完了)に subscribe します。独自のセットアップ内のリアクティブな自動化に最適。
:::

### 主なポイント

- **エージェントあたり複数のトリガー** — 上限なし。タイプを自由に組み合わせる
- **独立した発火** — 各トリガーは独自に評価。同じエージェント上のスケジュールトリガーは webhook トリガーを知らないし、気にしない
- **トリガーごとのフィルタリング** — 各トリガーは独自のフィルタ条件を持てる(例: webhook トリガーは \`event_type=charge.succeeded\` でのみ発火)
- **トリガーリネージ** — Lineage キャンバス(Events → Live Stream → Lineage)は、セットアップ全体でどのトリガー、どのエージェント、どのイベントが接続されているかをエンドツーエンドで表示
- **個別に一時停止** — エージェントの残りに触れずに 1 つのトリガーを無効化

### 仕組み

トリガーはエージェントの Settings タブで構成するか、Events ページのトリガーリストから追加します。実行エンジンはトリガー条件を独立して評価し、トリガーが一致するたびにエージェントに実行をディスパッチします。実行はトリガーペイロード(webhook ボディ、ファイルパス、コピーされたテキスト、上流の出力、イベントデータ)を入力としてエージェントに運びます。

:::tip
すべてのエージェントを Manual トリガーだけで開始しましょう。動作を信頼できたら、自動トリガーを 1 つずつ追加します — 何か問題があった場合に、どれが問題を引き起こしたかを切り分けられます。
:::
  `,

  "manual-triggers": `
## 手動トリガー

手動トリガーはすべてのエージェントのデフォルトです。エディターで \`Run\` をクリックするとエージェントが即座に開始されるか、タイトルバーのクイック実行ショートカット(フォーカスされたエージェント上で \`Ctrl+Enter\`)を使います。手動実行は開発とテストの方法です — スクリプトを直接実行して何をするか確認し、その後に cron エントリを追加するのと同等です。

毎回カスタム入力を渡せます。エージェントエディターはエージェントが入力を受け入れることを宣言しているとき、Run ボタンの隣に小さな入力フィールドを表示します。入力したものはトリガーペイロードとして渡されます。

### 主なポイント

- **構成なし** — 手動トリガーは常に利用可能
- **オプションの入力** — 入力を直接入力、構造化 JSON を貼り付け、または入力を必要としないエージェントには入力なしで実行
- **診断実行** — 手動実行はトレースで \`manual\` とタグ付けされるため、自動アクティビティのみを表示したい場合にコスト / メトリクスレポートからフィルタアウトできます
- **同時実行対応** — 手動実行はエージェントの同時実行制限を尊重。制限に達した場合、明確なメッセージでクリックが拒否されます

### 仕組み

手動トリガーはすべてのエージェントに暗黙的に存在します — オフにするトグルはありません(完全にロックアウトしたい場合はエージェント全体に \`Disable\` を使用)。エンジンは手動実行を自動と同じように扱います: 同じ実行パス、同じトレースキャプチャ、同じコスト計算。唯一の違いはトリガータグです。

:::tip
プロンプトのイテレーション中は手動実行を使いましょう。プロンプトを保存し、実行し、トレースを見て、編集。Lab arena は体系的な比較のため、手動はエディターでの高速フィードバックのためです。
:::
  `,

  "schedule-triggers": `
## スケジュールトリガー

スケジュールトリガーはエージェントを繰り返しの頻度で実行します — 毎時、平日 8 時、月の最初の月曜日、または書ける任意の cron 式。スケジュール UI は一般的なケース向けのプリセットショートカット(毎時、毎日、毎週)と、その他すべてのための生の cron フィールドを提供します。

スケジュールは構成可能なタイムゾーンを尊重します。デフォルトではエージェントはシステムタイムゾーンを使用しますが、トリガーごとに上書きできます — エージェントがどこに座っているかに関係なく「米国東部標準時 9 時に」実行する必要があるエージェントに便利です。

### 主なポイント

- **プリセットと cron** — 一般的な頻度から選ぶか、完全な cron 式を書く
- **トリガーごとのタイムゾーン** — IANA タイムゾーン名(\`America/New_York\`、\`Europe/Prague\`、\`UTC\`)、DST は自動的に処理
- **次回実行プレビュー** — トリガーは次の 3 つのスケジュール時刻を表示するので、cron 式を sanity-check できる
- **失わずに一時停止** — スケジュールトリガーを無効化しても削除されない。再開するには再有効化

### スケジュールのセットアップ

:::steps
1. **トリガー設定を開く** — エージェントの Settings タブ、または Events ページから \`Add trigger\` をクリックして Schedule を選ぶ
2. **プリセットを選ぶか cron を書く** — 「平日 8 時」には \`0 8 * * 1-5\`、一般的なケースにはプリセットを使用
3. **タイムゾーンを設定** — デフォルトはシステム。特定のビジネスカレンダーに紐づくエージェント用に変更
4. **次回実行プレビューを確認** — 3 つの今後の実行時刻が表示される。期待と一致するか検証
5. **保存** — トリガーは即座に武装し、「次回実行」カウントダウン付きでエージェントのトリガーリストに表示
:::

:::tip
スケジュールトリガーは見逃した実行をバックフィルしません。アプリが閉じられているか、マシンがスリープしているときにスケジュール時間が過ぎると、その実行はスキップされます。ミッションクリティカルなスケジュール作業には、クラウドデプロイ(Builder ティア)を実行して、オーケストレーターがサーバー側でスケジューリングを処理するようにしましょう。
:::
  `,

  "webhook-triggers": `
## Webhook トリガー

Webhook トリガーはエージェントがリッスンする一意のインバウンド URL を公開します。外部サービスがその URL に POST すると、ボディがトリガーペイロードになり、エージェントが実行されます。webhook をサポートする大半のサードパーティサービス(Stripe、GitHub、Shopify、Linear、Twilio、カスタム内部 API)は変更なしで動作します。

トリガーはリクエストボディ、ヘッダー、メソッドでのフィルタリングをサポートするため、単一のエンドポイントが、どのイベントが実際にエージェントを開始するかを選択できます。一般的なパターン: エージェントあたり 1 つの webhook URL、上流サービスからの特定のイベントタイプにフィルタリング。

### 主なポイント

- **トリガーごとに一意の URL** — 自動的に生成。エージェントやトリガー間で共有されない
- **フィルタ式** — JSONPath / ヘッダーマッチで、関心のあるイベントのみを受け入れる
- **リプレイエンドポイント** — 受信したすべての webhook は保持され、トリガー詳細ページから手動でリプレイ可能
- **Send Test** — ローカルエンドポイントに対してサンプルペイロードを POST する組み込みボタン。外部サービスなしでフィルタとエージェントの応答を検証できる
- **インバウンドとアウトバウンドは別** — 以下参照

### Webhook の接続

:::steps
1. **webhook トリガーを追加** — Events ページ → Add trigger → Webhook、エージェントにバインド
2. **生成された URL をコピー** — このトリガーに固有。トリガーを削除しない限り期限切れにならない
3. **外部サービスを構成** — URL をサービスの webhook 構成(Stripe Dashboard、GitHub リポジトリ設定など)に貼り付ける
4. **フィルタ式を設定** — サービスが発行するすべてのイベントでエージェントを実行しないように、特定のイベントタイプまたはペイロードシェイプに制限
5. **テスト** — Send Test をサンプルペイロードで使用(または上流サービスで実際のイベントをトリガー)、トレースを検査し、必要に応じてフィルタを調整
:::

### インバウンド vs アウトバウンド Webhook

webhook は 2 つの flavor があり、区別しておく価値があります:

- **インバウンド webhook(このトピック)** — 外部サービスがエージェントを開始するためにあなたを呼び出します。Stripe は課金が成功したときに ping を送り、GitHub は PR が開かれたときに ping を送ります。
- **アウトバウンド webhook(別の機能)** — *あなたの* エージェントが完了後に結果をチャンネルに送信します。Personas は Slack、Discord、Microsoft Teams、一般的な webhook URL へのファーストクラスのアウトバウンド配信を搭載し、Connectors タブでエージェントごとに構成されます。エージェントの出力は各チャンネル用に適切にフォーマット(リッチな Slack ブロック、Discord embeds、Teams カード)され、実行が完了するとディスパッチされます。

ほとんどの自動化は両方を使うことになります: インバウンド webhook がエージェントを開始し、エージェントが仕事を行い、アウトバウンドチャンネルが結果をチームが見ている場所に配信します。

:::tip
ローカル開発やプロダクション前の webhook には、実際の上流を構成するのではなく \`Send Test\` ボタンをサンプルペイロードで使いましょう。サードパーティサービスをラウンドトリップせずに、フィルタとプロンプトをはるかに速くイテレーションできます。
:::
  `,

  "clipboard-monitor": `
## クリップボードモニター

クリップボードモニターはシステムクリップボードを監視し、コピーされたコンテンツがルールに一致するとエージェントを発火させます。注文番号をコピーする — エージェントが調べます。外国語の文をコピーする — エージェントが翻訳します。顧客のメールをコピーする — エージェントがアカウントを取得します。

マッチングはシンプルなキーワード、regex パターン、またはコンテンツタイプヒューリスティック(メールアドレス、URL、電話番号、JSON 形式、数字、構造化 ID)で行えます。トリガーはクリップボードが変更されるたびにルールを評価し、ルールが一致したときのみ発火するため、実際に興味深いものをコピーするまでバックグラウンドで静かに座っています。

### 主なポイント

- **ルールベース** — トリガーあたり 1 つ以上のルールを定義。最初のマッチが勝つ
- **マッチモード** — キーワード、regex、または組み込みコンテンツタイプヒューリスティック(メール/URL/電話/JSON など)
- **デフォルトで静か** — 一致しないコピーは評価ログさえトリガーしない。一致のみがアクティビティを作成
- **出力モード** — デスクトップ通知として表示、Cockpit インボックスにプッシュ、または静かにエージェントのアクティビティフィードに書き込むだけ
- **プライバシー** — クリップボードコンテンツはローカルに留まる。エージェント自体が呼び出す AI プロバイダー以外には何もアップロードされない

### 仕組み

トリガーはアプリ起動時に OS クリップボードシステムに登録します。クリップボードが変更されると、新しいコンテンツがこのトリガーの各ルールに対して評価されます。最初の一致がコピーされたコンテンツを入力としてエージェントを発火させます。一致しないコピーはトレースを残さずにドロップされるため、モニターはアクティビティログを膨張させません。

:::tip
ルールは具体的にしましょう。すべての \`@\` シンボルに一致するクリップボードモニターは、意図しなかったコピーで発火します。完全なメール regex を使用するか、「顧客 ID のように見えるコピー」(自分の ID 形状に一致)にスコープしてください。
:::
  `,

  "file-watcher-triggers": `
## ファイルウォッチャートリガー

ファイルウォッチャートリガーは、指定したフォルダ内でファイルが表示、変更、または消えると発火します。CSV をフォルダにドロップするとエージェントが処理します。画像を「Process」ディレクトリに保存すると、OCR / 分類エージェントがそれに対して動作します。設定ファイルを変更すると、エージェントが前のバージョンと差分を取ります。

監視するフォルダはローカルファイルシステム上、または同期された場所(OneDrive、Dropbox、iCloud)にあります。フィルタは、無関係な変更(macOS の \`.DS_Store\` ファイルや一時的なエディタースワップファイルなど)でエージェントを実行しないように、ファイルタイプ / グロブパターンでイベントを絞り込みます。

### 主なポイント

- **任意のフォルダを監視** — ローカルまたは同期されたクラウドストレージ。サブフォルダの再帰はオプション
- **イベントタイプ** — 作成 / 変更 / 削除。1 つ、2 つ、またはすべてに subscribe
- **グロブフィルタ** — \`*.csv\`、\`**/invoices/*.pdf\`。否定パターンをサポート
- **デバウンス** — 連続した迅速な変更は 1 つのトリガーイベントに統合(保存と即座の保存フローでダブル発火しない)
- **ペイロード** — エージェントはファイルパスと(ファイルが十分に小さい場合)コンテンツをインラインで受け取る。そうでない場合は、エージェントがファイルアクセスツールで読めるパス

### 仕組み

トリガーは OS ネイティブのファイル監視 API(macOS の FSEvents、Windows の ReadDirectoryChangesW、Linux の inotify)を使用します。ウォッチャーはアプリが開いている間、エンジンプロセスで実行されます。イベントがトリガーのフィルタに一致すると、エンジンはファイルメタデータを入力としてエージェント実行をディスパッチします。エンジンはまた、ファイルウォッチャーイベントを **ambient producer** にルーティングします: 関連する ambient イベントに subscribe している任意のエージェントは、独自のウォッチャーなしで反応できます。

:::tip
ファイルウォッチャーを使用する各エージェント用に専用のドロップゾーンフォルダを作成しましょう。共有フォルダ(「Downloads」、「Desktop」)上のウォッチャーを混在させると、無関係なファイルをそこに保存したときに予期しない発火が発生します。
:::
  `,

  "chain-triggers": `
## チェーントリガー

チェーントリガーはエージェントをエンドツーエンドで接続します: エージェント A が成功裏に終了すると、エージェント B が A の出力を入力として開始します。これが多段自動化の構築方法です — 各エージェントは小さく焦点を絞り、チェーンがそれらをパイプラインに縫い合わせます。

チェーンは分岐(あるエージェントの出力が複数の下流エージェントに供給される)し、収束(複数の上流エージェントが 1 つの下流に供給される)できます。条件付きにすることもできます — トリガーは条件に一致する出力のみを転送するフィルタを持つことができ、重要なケースでのみ下流エージェントを実行します。

:::diagram
[Research Agent] --> [Writing Agent] --> [Formatting Agent] --> [Final Output]
:::

### 主なポイント

- **出力 → 入力配線** — 自動。下流エージェントのプロンプトは上流出力をそのまま見る(またはトランスフォーマーを構成すると変換)
- **分岐と収束** — 多対 1 と 1 対多のチェーンがサポートされる
- **条件付き転送** — チェーントリガーのフィルタ式により、特定の条件でのみ転送(出力に「error」が含まれる、またはフィールドがしきい値を超える)
- **失敗はチェーンを停止** — 上流エージェントが失敗した場合、下流のチェーンされたエージェントは実行されない。失敗はリネージビューに表示されるため、チェーンがどこで壊れたか正確にわかる
- **エンドツーエンドで可視** — Events → Live Stream → Lineage キャンバスはチェーンされたエージェントとライブ実行フローの完全なグラフを表示

### 仕組み

下流エージェントの Settings タブで Chain トリガーを追加し、上流エージェントを選びます。エンジンは下流エージェントを上流の完了イベントに subscribe します。上流が「成功で実行完了」を発行すると、エンジンは出力を下流の入力として転送します。条件付きフィルタは、下流実行がディスパッチされる前にサーバー側で評価されます。

:::tip
チェーン内の各エージェントは正確に 1 つのことを上手に行うべきです。3 つの小さな焦点を絞ったエージェントのチェーンは、1 つの大きな何でもエージェントよりはるかにデバッグしやすい — リネージビューでどのステージが失敗したかを見ることができ、チェーンの残りに触れずにエージェントをより良いバージョンに交換できます。
:::
  `,

  "event-based-triggers": `
## イベントベーストリガー

イベントベーストリガーは、内部 Personas イベントにエージェントを subscribe します。アプリ内でイベントを発行するもの — 別のエージェントの完了、認証情報の失効、プラグインの発火(Drive プラグインが Local Drive 内のファイルが変更されたときに \`drive.document.*\` イベントを発行するなど)、または manual-review ケースをフラグ付けするエンジン自体 — のどれもが、subscribe しているエージェントを駆動できます。

これは最も柔軟なトリガータイプです。webhook(外部システムから)やスケジュール(時計で発火)と違い、イベントは独自の Personas セットアップの内部から来ます。1 つの信号が明示的な配線なしに複数のエージェントにファンアウトできるイベント駆動のセットアップを構築します。

### 主なポイント

- **任意のイベントに subscribe** — エージェント完了イベント、プラグインイベント、エンジンイベント、他のエージェントが発行するカスタムイベント
- **ペイロード認識** — 各イベントはデータ(エージェントの出力、ファイルパス、認証情報 ID)を運ぶ。subscribe したエージェントはそれを入力として受け取る
- **1 対多** — 複数のエージェントが同じイベントに subscribe でき、発火時にすべてが並列で実行
- **フィルタ式** — ペイロードフィールドで制限(\`severity = critical\` のイベントのみで発火)
- **発見可能** — イベントレジストリは Events ページで閲覧可能。利用可能なイベントとそれらが運ぶフィールドを正確に確認できる

### 仕組み

下流エージェントに Event トリガーを追加し、レジストリからイベントを選びます。エンジンは起動時にエージェントを subscribe し、一致するイベントが発火するたびにイベントペイロードを使って実行をディスパッチします。プラグインが発行したイベントは、エージェントの視点からはエンジンが発行したものと同じに見えます — すべて同じバスを通って流れます。

:::tip
イベントベースのトリガーは、X を変更せずに「X が起こったら Y も」関係を構築する方法です。新しいエージェントにイベントトリガーを追加し、別のエージェントが発行するイベントに向け、新しい動作がリアクティブに発生します — 既存のエージェントは知らないし気にしません。
:::
  `,

  "combining-multiple-triggers": `
## 複数のトリガーを組み合わせる

エージェントは任意の数のトリガー、任意のタイプを持てます。ほとんどの本番エージェントには少なくとも 2 つあります: 手動トリガー(テストとアドホック呼び出し用)と 1 つ以上の自動トリガー(スケジュール、webhook、チェーン、イベント)。エージェントがスケジュール + webhook + チェーンの組み合わせを持つのは一般的です — 同じエージェントが日次バッチの一部として、リアルタイム webhook に応答して、チェーンパイプラインのステップとして実行できます。

複数のトリガーは干渉しません。各々独自のスケジュールやイベントで発火し、同じ瞬間に 2 つがトリガーされるとエージェントは 2 回実行されます(同時実行制限が許す範囲で)。各実行のトレースは、どのトリガーが開始したかをキャプチャします。

### 主なポイント

- **上限なし** — エージェントは数十のトリガーを持つことができる
- **独立した評価** — 各トリガーは独立して評価とディスパッチを行う
- **トリガーごとのフィルタリングと構成** — スケジュールには独自の cron、webhook には独自の URL など
- **トレース内のトリガータグ** — すべての実行は、開始したトリガーでタグ付けされるため、トリガーソースでアクティビティをフィルタリングできる
- **選択的な無効化** — 残りに触れずに 1 つのトリガーを無効化

### 仕組み

エージェントの Settings → Triggers タブには、アタッチされたすべてのトリガー、ステータス(有効/無効)、最終発火時刻が表示されます。\`Add trigger\` で新しいものを追加します。同じピッカーで 7 つのトリガータイプのいずれかを作成できます。無効化されたトリガーはリストに残るので、再構成せずに後で再有効化できます。

:::tip
便利なパターン: Manual トリガーを常にアクティブに保ち(デバッグ用)、各「本物」の自動トリガーを、同じ入力形状を取る兄弟 Manual トリガーとペアにします。これにより、調査したいときに任意の自動化されたペイロードを手動でリプレイできます。
:::
  `,

  "testing-and-debugging-triggers": `
## トリガーのテストとデバッグ

Events → Test タブはトリガーテスターです。任意のトリガーについて、サンプルペイロード(webhook ボディ、ファイルイベント、クリップボード文字列、イベントデータ)を送信し、外部サービスや実際のトリガー時刻を待たずに、エージェントが何を受け取り、どう応答するかを正確に確認できます。

発火したがエージェントが期待通りに実行しなかったトリガーについては、トリガーログはすべての評価を表示します: 一致したフィルタ、拒否されたフィルタ、ペイロード形状、ディスパッチ時刻。リネージキャンバス(Events → Live Stream → Lineage)はビジュアル相当 — セットアップ全体でライブのトリガー評価とディスパッチを表示します。

### 主なポイント

- **任意のトリガーをシミュレート** — ペイロードを貼り付けてエージェントの応答を確認
- **トリガーログ** — すべての発火試行が記録される。フィルタ拒否も含まれるので、何が一致しなかったかわかる
- **リネージキャンバス** — トリガー、エージェント、イベントのビジュアルグラフ。何かが発火しているときにライブフローインジケーター付き
- **webhook 用 Send Test** — サンプルボディをローカルエンドポイントに POST する組み込みボタン
- **リプレイ** — 過去のトリガー発火は、まったく同じオリジナルペイロードでリプレイできる。「この Stripe webhook がエージェントに再度ヒットしたらどうなるか」に便利

### トリガーをステップバイステップでデバッグ

:::steps
1. **トリガーが有効か確認** — エージェントの Settings → Triggers タブ。ミュートされたアイコンはトリガーが無効
2. **トリガーログをチェック** — Events → Test → トリガーでフィルタリングされた Logs。ディスパッチしなかった評価を探す
3. **ペイロードに対するフィルタを検査** — トリガーが評価されたがディスパッチしなかった場合、フィルタ式が拒否している。ペイロードをコピーしてフィルタを明示的にテスト
4. **ディスパッチがエージェントに届いたか確認** — 実行トレースにトリガータグが表示されるはず。実行が表示されない場合、トリガーはディスパッチされていない(フィルタ問題、同時実行制限、または無効化されたエージェント)
5. **リネージキャンバスを使用** — チェーンまたはイベントトリガーの場合、Lineage を開いてパスをトレース。フローがどこで中断されたかが見える
:::

:::tip
「トリガーが発火しない」はほとんどの場合次のいずれかを意味します: トリガーが無効、フィルタが厳しすぎる、エージェントが無効、または外部サービスが思っているものを実際に送信していない。トリガーログは 1 分以内にこの 4 つを区別します。
:::
  `,
};
