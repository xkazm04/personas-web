export const content: Record<string, string> = {
  "local-vs-cloud-execution": `
## ローカル vs クラウド実行

Personas エージェントは 2 つの場所で実行されます: ローカルマシン上(デスクトップアプリ独自のエンジン)またはリモートオーケストレーター上(弊社がクラウド管理、または自社インフラ上の BYOI)。ローカルはデフォルトで、すぐに動作します。クラウドはオプトイン(Team / Builder ティア)で、マシンがオンでなくても 24 時間 365 日の可用性を可能にします。同じエージェントのプロンプト、ツール、認証情報がどちらの環境でも動作 — 切り替えはデプロイメントの決定であり、再設計ではありません。

決定要因は通常、アップタイムと可観測性の要件です。ローカルは、開発、テスト、探索的なエージェント、作業を見ているときに最適です。クラウドは、スケジュールされた夜間実行、就寝中にもリーチ可能である必要のある webhook エージェント、および「ラップトップが閉じていた」が失敗モードであってはならない任意の本番グレードの自動化に正しい選択です。

:::compare
**ローカル実行** [default]
デスクトップアプリのエンジンで実行。アプリが開いている間利用可能。セットアップなし。データと認証情報はマシンを離れない。構築するのと同じ UI で完全なライブ可観測性。開発、テスト、監督された作業、プライバシーに敏感なものに最適。
---
**クラウド実行**
オーケストレーター(マネージドクラウドまたは BYOI)上で実行。ローカルマシンに関係なく 24 時間 365 日利用可能。セットアップは 1 回限り。データと認証情報は転送中にオーケストレーターへ TLS で暗号化、その上で保存時にも暗号化。結果はデスクトップに同期。スケジュール、webhook、本番グレードの無人作業に最適。
:::

### 仕組み

ローカルエージェントはアプリ内実行エンジンによってディスパッチされます — アプリ内のすべてが使用するのと同じパス。クラウドエージェントはデプロイされます: エージェントの完全な構成(プロンプト、ツール、認証情報の参照、トリガー)がオーケストレーターに送信され、トリガーをサーバー側で処理する長寿命のエージェントプロセスを実行します。結果はデスクトップアプリにストリームバックされ、ローカル実行と同じモニタリングビューに表示されます。

:::tip
ローカルで開発・テストし、動作するものをクラウドにデプロイします。ローカルエンジンは最速の edit-test ループを持ち、クラウドはスケジュールや可用性が重要なエージェントを置く場所です。グローバルにどちらかを選ぶ必要はありません — 典型的なセットアップでは、ほとんどのエージェントがローカルで、本番のものが数個クラウドにあります。
:::
  `,

  "connecting-to-the-cloud-orchestrator": `
## クラウドオーケストレーターへの接続

Deployment → Cloud Deploy を開いてオーケストレーターに接続します。2 つのパス: **マネージドオーケストレーター**(弊社がホスト、アカウントで認証して 30 秒で完了)または **BYOI**(自社インフラ上でオーケストレーターをホスト、デスクトップアプリをエンドポイントに向け認証キーを提供)。どちらの方法でも、接続はマシンごとに 1 回限りで、アプリ再起動間で永続します。

接続されると、すべてのエージェントの Settings タブに「Deploy to cloud」オプションが追加されます。デプロイメントをトリガーすると、エージェントの構成がオーケストレーターにアップロードされ、その長寿命のサーバー側プロセスが開始されます。クラウドエージェントはローカルと同じモニタリングビューに小さなクラウドアイコンでタグ付けされて表示されます。

:::steps
1. **Deployment → Cloud Deploy を開く** — サイドバー → Deployment → Cloud Deploy
2. **環境を選ぶ** — Managed Cloud(ワンクリックサインイン)または BYOI(オーケストレーター URL と認証キーを入力)
3. **BYOI の場合**: オーケストレーター URL と認証トークンを貼り付け。ウィザードは接続テストを実行し、オーケストレーターバージョンの互換性を検証
4. **Managed の場合**: 「Sign in」をクリック。OAuth フローが開き Personas アカウントに対して認証
5. **保存** — 接続は永続。エージェントは Settings タブに「Deploy to cloud」オプションを表示するようになる
:::

:::warning
BYOI 認証トークンを他の認証情報と同じように扱ってください: ボールトに保存(Connections → Credentials → Custom)、チャットに貼り付けたり、バージョン管理にコミットしたりしないでください。トークンを持つ者は、オーケストレーター上の任意のエージェントをデプロイおよびアンデプロイできます。
:::

### 仕組み

オーケストレーターは長期実行サーバープロセス(環境ごとに 1 つ)で、デプロイされたエージェント構成を保持し、スケジュール、webhook イベント、オンデマンドで実行します。デスクトップアプリとオーケストレーター間の通信は、相互認証付きの TLS 上です。デプロイされたエージェントの認証情報は、デプロイ時にオーケストレーターのテナントごとのキーで暗号化され、実行時にオーケストレータープロセス内でのみ復号されます。

:::tip
何かをデプロイする前に接続をテストしてください。ウィザードの接続テストは、バージョン互換性と到達可能性を検証します — 失敗した場合、3 つのエージェントをデプロイしようとした後よりも、診断するのが今の方がはるかに簡単です。
:::
  `,

  "deploying-an-agent-to-the-cloud": `
## エージェントをクラウドにデプロイする

オーケストレーターが接続されていれば、任意のエージェントをデプロイするのは Settings タブの 1 つのボタンです。デプロイアクションは、エージェントの完全な構成(プロンプト、ツール、認証情報参照、トリガー定義、設定)をパッケージ化し、TLS 上でオーケストレーターに送信します。オーケストレーターは検証し、エージェントを設定し、サーバー側でトリガーの処理を開始します。最初の実行は通常、数秒以内に発生します。

同じエージェントのローカルとクラウドのコピーは、すべてのデスクトップ ↔ クラウド調整を処理する自動同期システムで同期されます。ローカルでエージェントのイテレーションを続け、準備ができたら再デプロイできます。2 つの環境のどちらかを選ぶ必要はありません。

:::steps
1. **オーケストレーター接続を確認** — Deployment → Cloud Deploy は「Connected」を表示するはず
2. **エージェントを開く** — Agents ページ → デプロイしたいもの
3. **Settings タブ → Deploy to Cloud** — デプロイメントセクションのボタン
4. **デプロイメントサマリーをレビュー** — 出荷される認証情報、武装されているトリガー、モデル選択、フェイルオーバー設定。すべてがローカルでテストしたものと一致するはず
5. **Deploy を確認** — オーケストレーターが構成を受信、検証、エージェントを設定。ステータスは数秒で「Deployed」に切り替わる
6. **ダッシュボードで検証** — Overview → Activity でエージェントがクラウドアイコン付きで表示。次のスケジュール / webhook イベントはクラウドインスタンスにルーティング
:::

:::warning
クラウドエージェントは、ローカルボールトを直接ではなく、クラウド側ボールトの認証情報を使用します。デプロイアクションは認証情報 *参照*(暗号化)を出荷し、オーケストレーターはサーバー側でそれらを解決します。認証情報がローカル専用かレプリケートされていない場合、デプロイは「クラウドで認証情報が利用できない」警告を surface し、完了する前にレプリケートまたは代替を選ぶよう求めます。
:::

### 仕組み

デプロイメントはアトミック: オーケストレーターは完全な構成を受け入れてエージェントがライブになるか、(特定の理由で)拒否してサーバー側で何も変わらないかのどちらかです。デプロイされると、オーケストレーターはトリガー評価を所有 — ローカルアプリはそのエージェントに対してスケジュール / webhook を発火しません(そうでないと重複が発生します)。デスクトップアプリからの手動実行は、同じ接続上でクラウドインスタンスにルーティングされます。

:::tip
クラウドを始めるときは、最初にスケジュールエージェントをデプロイしてください。24 時間 365 日のアップタイムから最も恩恵を受け、検証が最も簡単です(ラップトップが開いているかに関係なく、期待されるスケジュールで実行が着地するのを見ることができます)。
:::
  `,

  "cloud-execution-monitoring": `
## クラウド実行のモニタリング

クラウドエージェントは、ローカルエージェントと同じ Overview ページから表示可能 — 同じ Activity フィード、同じ Health タブ、同じ Usage 内訳。小さなクラウドアイコンがクラウドエージェントをローカルから区別します。任意のクラウド実行にクリックすると、ローカル実行と同じように完全なトレース: レンダリングされたプロンプト、モデル呼び出し、ツール呼び出し、出力、コストを取得します。

デスクトップアプリは開いている間オーケストレーターを継続的にポーリングし、接続中はライブイベントストリームに subscribe するため、表示されるのは数分ではなく秒単位の遅延でライブ状態です。アプリが閉じているとき、オーケストレーターは独自にすべてを動かし続けます。アプリを後で開くと、オーケストレーターの権威ストアからローカル状態を catch up します。

### 主なポイント

- **統一されたモニタリング surface** — ローカルとクラウドのエージェントは同じ Activity / Health / Usage ビューを共有
- デスクトップが接続している間の **ライブイベントストリーミング**。オーケストレーター側の永続化により、オフライン時に何も失われない
- **クラウドアイコン** がクラウド常駐エージェントを区別
- **クラウドへのコスト帰属** — 使用チャートはローカルとクラウドの支出の両方を含み、環境で分類
- **再接続時の catch up** — 長時間のオフライン後にアプリを開くと、オーケストレーターからのすべての見逃したイベントが同期

### 仕組み

クラウドエージェントは、ローカルエージェントと同じ実行とイベントレコードを発行 — オーケストレーターはサーバー側で保存し、接続時にデスクトップアプリにレプリケートします。Activity フィードはローカルとクラウドのイベントストリームを時系列順にマージするため、混合ローカル + クラウドセットアップは 2 つの並列ではなく 1 つの統一ビューに見えます。

:::tip
1 日目からクラウドエージェントに日次予算上限を設定してください。クラウドエージェントには、ローカル手動実行が持つ「これが起こっているのを見ている」という暗黙のチェックがありません。日次キャップは、夜間の暴走プロンプトに対するセーフティネットです。
:::
  `,

  "github-actions-integration": `
## GitHub Actions 統合

エージェントは Connectors タブの GitHub ツール経由で GitHub Actions ワークフローをトリガーでき、GitHub Actions は標準の webhook トリガー経由でエージェントをトリガーできます。2 つのパターンはうまく結合します: GitHub イベント(PR が開かれた、main へのプッシュ、リリースがタグ付けされた)が webhook を発火させて Personas エージェントを開始し、エージェントは自分の仕事を行い、(必要なら)出力の一部としてワークフローをトリガーします。

GitHub コネクターはカタログ(Connections → Catalog → Developer Tools → GitHub)で出荷されています。認証は OAuth または fine-grained PAT — エージェントが読み取りアクセスのみが必要な場合は OAuth が推奨。ワークフローのディスパッチなどの書き込み操作には PAT がうまく動作します。

### 主なポイント

- **GitHub → Personas をインバウンド webhook 経由で** — 標準の webhook トリガー。エージェントの URL に POST するように GitHub を構成
- **Personas → GitHub を GitHub ツール経由で** — エージェントはワークフローのディスパッチ、PR でのコメント、issue のオープン、GitHub API が公開する任意のものを行える
- **スコープ付き認証** — 読み取り中心のエージェントには OAuth、書き込み操作には fine-grained PAT。エージェントごとに最小スコープ
- **ライブステータス同期** — エージェントトレースは workflow_dispatch リクエストと GitHub の応答を表示。必要に応じて、エージェントはワークフローが完了するのを待てる

### 仕組み

:::diagram
[GitHub イベント] --> [インバウンド webhook] --> [エージェントが決定] --> [GitHub ツールがワークフローをディスパッチ] --> [ワークフロー結果がトレースに戻る]
:::

GitHub ツールは GitHub REST/GraphQL API をラップし、エージェントに高レベルアクション: 「ワークフローをディスパッチ」、「PR にコメント」、「issue を開く」、「PR をマージ」などを公開します。エージェントのプロンプトは、トリガーに基づいて取るべきアクションを名指します。ツールは認証、ペイロード構築、応答処理を処理します。

:::warning
GitHub プランがサポートしている場合は常に、classic PAT より fine-grained PAT を使用してください。Classic PAT は広範な org 全体の権限を付与します。Fine-grained PAT は特定のリポジトリと特定の権限スコープに制限されるため、トークンが漏れた場合の影響範囲が劇的に厳格化されます。
:::

:::tip
ターゲットとしてリスクの低いワークフロー — メッセージを投稿するだけの「Slack に通知」ワークフローなど — から始めてください。エージェント → GitHub Actions のハンドオフが証明されたら、より高リスクのターゲット(デプロイ、リリースカットなど)に卒業します。
:::
  `,

  "gitlab-ci-cd-integration": `
## GitLab CI/CD 統合

Personas は 2 つの方法で GitLab と統合します: API レベルアクセス(パイプラインステータス、MR コメント、issue 管理)をエージェントに与える直接 GitLab プラグイン、および既存のパイプライン内のステップとして Personas エージェントを実行する GitLab CI YAML エクスポート。両方が出荷 — チームのワークフロー形状に合うものを選んでください。

プラグイン(Plugins → GitLab)は API 側の統合を処理: インストール、認証、エージェントは高レベルアクション(パイプライン開始、MR にコメント、issue 管理)を持つ \`gitlab\` ツール surface を取得します。CI YAML エクスポートは反対方向 — エージェントは GitLab CI パイプラインのステップになり、GitLab runner で実行され、結果は後続のステップに転送されます。

### 主なポイント

- **GitLab プラグイン** — API レベル統合。エージェントは Connectors タブからツールとして GitLab を使用
- **CI YAML エクスポート** — エージェントは GitLab パイプラインのステップになる。GitLab runner で実行
- **双方向** — GitLab イベントはエージェントをトリガーでき(webhook)、エージェントは GitLab パイプラインをトリガーできる(プラグイン)
- **トークンスコープ** — 必要最小限の権限にスコープされたプロジェクトアクセストークンまたはグループアクセストークンを使用
- **パイプラインイベントをトリガーとして** — \`Pipeline succeeded\`、\`Pipeline failed\`、\`MR merged\` はすべて webhook トリガー経由で消費可能

### 仕組み

プラグインは認証情報ボールトに保存された GitLab API トークンを使用します。エージェントが GitLab ツールアクションを呼び出すと、エンジンは API 呼び出しをディスパッチし、応答をキャプチャし、モデルの次のターンのツール結果として戻します。

CI エクスポートの場合: エージェントの Settings タブ → Export → GitLab CI YAML を開きます。ウィザードは、エージェントを CI 実行可能形状にラップするジョブ定義を生成(通常は Personas CLI とエージェントの参照を含む Docker イメージ)。生成された YAML をリポジトリの \`.gitlab-ci.yml\` にコミット。エージェントは他の任意の CI ジョブと並んでパイプラインの一部として実行されます。

:::warning
エクスポートされた CI YAML は AI プロバイダーキーなどの認証情報変数を参照します。これらをプロジェクト設定で **マスク済み、保護された** GitLab CI/CD 変数として定義してください — パイプライン YAML はリポジトリに存在し、読み取りアクセスのある全員に見えるため、YAML ファイル自体にシークレットを決してハードコードしないでください。
:::

:::tip
プラグインはほとんどのチームにとってより軽量なオプションです。CI YAML エクスポートは、エージェントが GitLab runner 内で実行する必要がある場合(ネットワーク隔離、内部ネットワークリソース、コンプライアンス義務化されたインフラ)に最も有用 — それ以外の場合、プラグインを使うと、可観測性とデバッグが最も豊富な Personas にエージェントを保てます。
:::
  `,

  "n8n-workflow-integration": `
## n8n ワークフロー統合

n8n は人気のあるオープンソースワークフロー自動化ツールで、Personas は双方向に統合します。既存の n8n ワークフローを Personas にテンプレートとしてインポート(Templates → n8n Import)できます — インポートウィザードはワークフロー JSON を解析し、n8n ノードを同等の Personas エージェント、コネクター、トリガーにマッピングします。HTTP/webhook ノードを使用してエージェントのインバウンド webhook URL を呼び出すことで、n8n *から* Personas エージェントを呼び出すこともできます。

n8n インポートは一方向で 1 回限りです: ワークフローの *形状* を Personas に持ち込みますが、n8n オリジナルを同期し続けません。インポート後、インポートされたパイプラインは独立して編集可能です。

### 主なポイント

- **n8n → Personas インポート** — Templates → n8n Import。ワークフロー JSON を解析、ノードを Personas 同等にマップ
- **Personas → n8n トリガー** — n8n の HTTP/webhook ノードは、エージェントの webhook トリガー URL に POST 可能
- **n8n → Personas トリガー** — n8n は、n8n ワークフローの一部として Personas エージェント webhook を呼び出せる。エージェントの応答(構成可能)は n8n に戻る
- **同期されない** — インポートされたパイプラインは n8n ソースから分岐。インポートを 1 回限りの出発点として扱う
- **マップされたノードカバレッジ** — インポーターは一般的なノード(HTTP、function、IF、switch)を処理。エキゾチック / コミュニティノードは手動完成用のプレースホルダーとしてインポートされる可能性がある

### 仕組み

インポートウィザードは n8n ワークフロー JSON(n8n から「Download」でエクスポート)を読み、各ノードを最も近い Personas 相当(HTTP ノード → ツール、function ノード → エージェント、IF/switch → 条件付きルーティングなど)にマッピングし、結果をプレビューする前にパイプラインとしてステージングします。マッピングはベストエフォート: インポーターが自信を持ってマップできないものは、記入するためのノート付きのプレースホルダーになります。

逆方向の場合、Personas エージェントの webhook URL は単なる URL — 任意の n8n HTTP ノードはそれを呼び出せます。入力をリクエストボディとして渡します。エージェントは処理し、(オプションで)出力で同期的に返信します。

:::tip
n8n は「サービス間でデータを移動する」配管に優れ、Personas は「考える」 — 分析、決定、執筆に優れています。最強の組み合わせワークフローは、すべてを 1 つで行おうとするのではなく、オーケストレーションには n8n、AI 駆動の決定ポイントには Personas エージェントを使用します。
:::
  `,

  "byoi-bring-your-own-infrastructure": `
## BYOI — Bring Your Own Infrastructure

BYOI(Builder ティア)は、マネージドクラウドを使う代わりに、オーケストレーターを自分で実行することを意味します。オーケストレーターソフトウェア(Docker イメージと Kubernetes Helm chart として提供)を自社インフラにインストールし、好みに応じて構成(認証、ストレージ、ネットワーキング)し、デスクトップアプリをオーケストレーター URL に向けます。その時点から、エージェントのデプロイはマネージドクラウドと同じように動作します — ハードウェア上で実行されるだけです。

BYOI は、データ主権が重要(規制環境、顧客データ隔離、エアギャップネットワーク)、活用したい既存インフラがある(マネージドホスティングに加えて支払う代わりに)、または実行環境を完全に制御したい(カスタムネットワーキング、特定の可用性保証、既存の可観測性スタックとの統合)場合に正しい選択です。

### 主なポイント

- **セルフホストオーケストレーター** — Docker イメージ + Helm chart がリリースごとに公開
- **データ主権** — 実行データ、認証情報、トレースはインフラを離れない
- **同じエージェントセマンティクス** — BYOI オーケストレーターにデプロイされたエージェントは、マネージドクラウドと同じように動作
- **あなたの認証、あなたのストレージ、あなたのネットワーク** — オーケストレーターは既存のアイデンティティプロバイダー、データベース、ネットワークポリシーと統合
- **Builder ティア機能** — オーケストレーターソフトウェアライセンスのために Builder サブスクリプションが必要

### 仕組み

オーケストレーターは長期実行サーバープロセスとして動作します。Docker イメージは単一ノードデプロイメント用に自己完結型。Helm chart は共有ストレージ付きの HA マルチノードセットアップをサポートします。認証は OIDC プロバイダーと統合するため、既存の SSO を使用できます。ストレージは Postgres(マネージドまたはセルフホスト)を使用。認証情報ボールト暗号化キーは選択の KMS(Vault、AWS KMS、GCP KMS、Azure Key Vault)に存在します。

BYOI オーケストレーターへのエージェントのデプロイは、デスクトップアプリの観点からマネージドクラウドと同じ — 同じ UI、同じフロー、同じ可観測性。オーケストレーターエンドポイントは、弊社ではなく自社のインストールを指すように構成されているだけです。

:::info
BYOI は真のインフラ作業です。オーケストレーターソフトウェアはよく文書化され、Helm chart はほとんどのセットアップを処理しますが、本番サーバーソフトウェアの実行に慣れた人が必要です。その能力のないチームには、マネージドクラウドが良い出発点 — 要件が変わった場合は後で BYOI に切り替えられます。
:::

:::tip
慣れていない場合は、まずステージング環境で BYOI を実行してください。セットアップガイドには、オーケストレーター + Postgres + Vault を 1 台のマシンで実行する「最小ローカルスタック」Docker Compose が含まれます — 本番ハードウェアにデプロイする前に動く部品を動作させるのに最適です。
:::
  `,

  "syncing-desktop-and-cloud": `
## デスクトップとクラウドの同期

クラウドオーケストレーターにエージェントがデプロイされていると、デスクトップアプリは 2 つの間の状態を自動的に同期します。デプロイされたエージェントへのローカル編集(プロンプト変更、設定の調整、認証情報のローテーション)は保存時にオーケストレーターにプッシュします。クラウド側のイベント(実行結果、トリガー発火、ヘルス変更)はデスクトップに同期し、モニタリングビューに表示されます。

同期はデスクトップが接続されている間バックグラウンドで継続的に実行。アプリがオフラインのとき、ローカル変更はキューに入り、再接続時にプッシュ。クラウドイベントはサーバー側に蓄積され、再接続時にストリームダウンします。ステータスバーは小さなインジケーターで同期状態を表示(green = 完全同期、amber = 同期中 / キューに入った変更、red = 注意が必要な同期エラー)。

### 主なポイント

- **双方向、自動** — ローカル変更は保存時にプッシュ。クラウドイベントは継続的にストリームダウン
- **オフライン耐性** — ローカル変更はオフライン中にキューに入り再接続時にプッシュ。クラウドはイベントを catch up 用に保持
- **競合検出** — 同じエージェントがローカルとリモートで編集された場合(例: 同じオーケストレーターを使用するチームメイト)、デスクトップはコミット前に解決を促す
- **ステータスインジケーター** — 下部バー要素はライブ同期状態を表示
- **手動同期** — インジケーターをクリックして明示的な同期トリガー。切断する直前に有用

### 仕組み

同期はリソースごとのバージョンベクトルを使用します。各エージェント、認証情報、トリガー、実行レコードは変更時に増分するバージョンを運びます。同期は「自分のバージョンを送信、新しいものを受信」 — 効率的、競合認識。競合(稀ですが、共有オーケストレーターセットアップで可能)は解決プロンプトとして surface — どのバージョンが勝つかを選ぶか、手動でマージします。

:::tip
意味のある変更後に同期インジケーターを一目見てください。Green は、アプリを閉じてもクラウドが最新を持つと信頼できることを意味します。Amber は変更が進行中であることを意味 — 確認したい場合は切断する前に数秒待ちましょう。
:::
  `,

  "cloud-troubleshooting": `
## クラウドのトラブルシューティング

ほとんどのクラウド問題は小さなセットに分類されます: オーケストレーター到達不可(ネットワーク / ファイアウォール / オーケストレーターダウン)、認証情報の不一致(エージェントが使用する認証情報がオーケストレーター側にレプリケートされていない)、バージョンの不一致(オーケストレーターがデスクトップより古いリリース、機能が欠落)、または非同期構成(ローカルに未保存の変更がある、まだプッシュされていない)。Deployment → Cloud Deploy ステータスページは最高の診断 surface — オーケストレーターのヘルス、同期状態、特定の失敗理由付きのエージェントごとのデプロイメントステータスを表示します。

エージェントレベルの問題(エージェントはデプロイされているが実行されていない、ローカルでは成功するがクラウドでは失敗する実行)については、エージェントの Health タブはクラウドにもローカルと同じ診断を表示 — 認証情報ステータス、最近の失敗理由、構成の完全性。実行トレースは、実行がクラウドで実行されたかローカルで実行されたかも表示するため、「クラウドのみ」の問題を素早く分離できます。

### よくある問題と修正

| 症状 | 考えられる原因 | 修正 |
|---|---|---|
| エージェントがスケジュールで実行されない | オーケストレーターが到達不可、またはクラウド側でトリガーが無効 | Deployment ステータスを確認、トリガー状態が古い場合は再デプロイ |
| クラウド初回実行で認証情報エラー | 認証情報がオーケストレーターにレプリケートされていない | Deployment → Cloud Deploy → "Sync credentials"、エージェントの Connectors タブを検証 |
| 結果がデスクトップに表示されない | 同期が一時停止、または実行中にアプリがオフライン | 同期インジケーターをクリック、再接続時にイベントがストリームダウン |
| クラウドエージェントがローカルより遅い | デプロイ時に異なるモデル / プロバイダーが構成された、またはエージェントから AI プロバイダーへのネットワークレイテンシ | Cloud Deploy 詳細ビューでエージェントの有効な構成を確認 |
| デプロイで「Version mismatch」エラー | オーケストレーターが古いリリース | オーケストレーターをアップグレード(BYOI)、またはマネージドクラウドのロールアウトを待つ |

### 仕組み

Deployment ステータスページは、デスクトップが接続されている間オーケストレーターを継続的にポーリングし、結果を単一のダッシュボードとしてレンダリングします。各デプロイ済みエージェントには、特定の問題を名指したリソースごとのステータス(healthy / degraded / unreachable)があります。ほとんどの問題には、ステータス行から直接ワンクリックで解決策が提供されます。

:::warning
「再デプロイ」は多くのクラウド問題に対する最も簡単な修正ですが、*現在のローカル状態* をオーケストレーターにプッシュします。レビューしていないローカル変更がある場合(または共有オーケストレーターでクラウドにローカルに届いていない変更がある場合)、再デプロイはそれらを上書きする可能性があります。常に同期状態を最初にチェック — amber の場合、再デプロイ前に同期を解決してください。
:::

:::tip
最も一般的なクラウド問題は「認証情報をクラウドボールトにレプリケートし忘れた」です。任意のエージェントをデプロイする前に、デプロイウィザードは認証情報の可用性を事前チェックして警告します。その警告を却下するのではなく注意を払ってください、そしてほとんどのクラウド側認証情報エラーは消えます。
:::
  `,
};
