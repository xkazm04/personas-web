export const content: Record<string, string> = {
  "the-monitoring-dashboard": `
## 监控仪表盘

Overview 页面是你了解所有 agent 活动的指挥中心。Dashboard 选项卡默认打开并显示 KpiTile 网格 — 每个指标一个磁贴(成功率、总运行次数、总成本、平均持续时间、活跃 agent、今日失败数等)。每个磁贴有三种密度模式(紧凑 / 标准 / 详细),你通过点击磁贴在它们之间切换;在你想要快速数字与想要趋势图和细分时很有用。

KpiTile 下方,Overview 呈现实时活动、最近的失败,以及你订阅的通知。该页面上的所有内容都可以按 agent、按组和按时间范围过滤 — 同一过滤集适用于每个面板,这样你可以一键将整个仪表盘范围限定为"本周,仅我的 Marketing agent"。

| 面板 | 显示内容 |
|---------|--------------|
| **KpiTiles** | 成功率、运行次数、成本、持续时间、失败计数、活跃 agent — 每个有三个密度级别 |
| **Activity feed** | 跨所有 agent 的执行实时流,可滚动、可搜索、点击进入详情 |
| **Notifications** | 订阅的警报(失败、预算上限、手动审查、异常),可点击进入有问题的运行 |
| **Health snapshot** | 每 agent 健康汇总 — 快速扫描黄色或红色 |

### 工作原理

Overview 页面从应用其余部分使用的相同执行和事件存储中读取,所以你看到的总是实时状态。过滤器和密度偏好在会话间持久化;你设置一次,仪表盘会记住。点击任何 KpiTile 以深入查看每 agent 细分,点击任何活动行以打开执行详情模态框。

:::tip
标题栏的通知铃铛是从应用任何地方到最新执行详情的一键快捷方式。你不需要手动导航到 Overview 来进行常规"刚刚发生了什么?"检查。
:::
  `,

  "execution-logs": `
## 执行日志

每个 agent 运行都产生一个执行日志:触发器载荷、发送到模型的渲染 prompt、模型响应、每个工具调用(带参数和结果)、最终输出、持续时间、成本以及任何错误。日志是不可变的 — 它们写入一次并无限期保留。Activity 选项卡(在编辑器上是每 agent,在 Overview 上是全局)是入口点。

每个日志条目是列表中的一行摘要;点击它会打开完整的详情模态框,包含上述所有字段。从那里你可以复制任何字段,使用相同的输入重放运行,或跳转到相关的追踪视图以进行逐步调试。

### 关键点

- **完整捕获** — 输入、prompt、响应、工具调用(带参数和结果)、输出、持续时间、成本、错误
- **不可变历史** — 运行完成后日志永不更改;如果 agent 的 prompt 后来被编辑,旧运行仍然显示当时发送的内容
- **从任何运行重放** — 使用原始输入重新运行 agent;对于在先前失败的载荷上验证修复很有用
- **按触发器标记** — \`manual\`、\`schedule\`、\`webhook\`、\`chain\` 等,这样你可以按来源过滤活动
- **手动审查标记** — agent 自己标记为待审查(通过 \`manual_review\` 指令)的运行会获得徽章,这样你可以快速找到它们

### 工作原理

执行存储是本地 SQLite,在运行进行时以事务方式写入。详情模态框内的追踪选项卡将每个步骤展开为其子事件(模型 token 流、分派的工具调用、收到的工具结果、采取的决策分支)。按日期范围、agent、触发器类型、状态、business_outcome 或对输入/输出的全文过滤。

:::tip
当 agent 产生意外输出时,追踪选项卡 — 而不是输出 — 是答案所在的地方。寻找返回错误数据的工具调用,或选择错误分支的模型决策。输出是症状;追踪是原因。
:::
  `,

  "real-time-activity-feed": `
## 实时活动 feed

活动 feed 向你显示所有 agent 中当前发生的事情。每个 agent 处理其任务时,更新实时出现 — 就像看实时记分牌一样。你在结果发生的那一刻就看到它们,无需刷新或检查单个 agent。

当你有许多 agent 同时运行或想要在执行时观察关键自动化时,这特别有用。

### 关键点

- **实时更新** — 在发生时查看 agent 活动,无需刷新
- **所有 agent** — 一个 feed 覆盖你设置中的每个运行的 agent
- **时间戳条目** — 每个更新显示发生的确切时间
- **状态更改** — 实时查看 agent 何时启动、完成、成功或失败

### 工作原理

从监控仪表盘或侧边栏打开活动 feed。Agent 工作时更新自动流入。每个条目显示 agent 名称、操作、时间戳和结果。点击任何条目 — 或标题栏中的通知铃铛 — 直接在 Overview › Activity 选项卡上打开完整执行详情模态框,你可以在那里查看追踪、渲染的 prompt、输入、输出和任何错误。Feed 本身是可滚动和可搜索的。

:::tip
在测试新 agent 时,将活动 feed 保持在侧面板中打开。观察实时输出有助于你立即发现问题并更快迭代。对于日常使用,标题栏的通知铃铛是最快路径 — 它总是打开最新的执行详情,而你无需导航。
:::
  `,

  "cost-tracking-per-agent": `
## 每 agent 成本跟踪

每个 AI 提供商按 token 收费,Personas 用确切的 token 数量、模型和提供商标记每次运行,所以每 agent 成本总是已知的。Overview → Usage 显示一个可排序的列表,包含选定时间窗口内每个 agent 的成本 — 天、周、月或自定义范围 — 加上趋势箭头,这样你可以一目了然看到哪些 agent 的成本在攀升。

深入任何行以获得细分:每运行成本分布(中位数 vs. p95)、当 agent 配置了故障转移时按模型的成本、总 token 数(输入 vs. 输出),以及随时间的趋势图。如果 agent 的成本在悄悄上升,这是首先呈现它的地方。

### 关键点

- **每 agent 细分** — 每次运行都归属于其 agent
- **可过滤时间窗口** — 今天、本周、本月、所有时间或自定义范围
- **每运行成本分布** — 中位数、p95、最大值;揭示一个昂贵的异常值是否在主导总数
- **Token 细分** — 输入 vs. 输出 token,这样你可以判断 agent 是在大量阅读还是大量产生
- **趋势箭头** — 周环比变化显示在每个 agent 旁边,这样成本回归立即浮现

### 工作原理

成本计量器在运行期间随着 token 流入而实时跳动。当运行完成时,最终成本被确定并与执行日志一起持久化。Usage 视图从此存储聚合,所以更改时间范围过滤器只是重新查询相同的数据 — 没有单独的"成本核算"作业在运行。

:::tip
如果单个 agent 主导了你的成本,每运行分布比总数更有用。高中位数意味着 prompt 持续昂贵(查看 prompt 大小和工具调用计数)。中位数正常但 p95 高意味着罕见的异常值(在追踪历史中查找异常输入)。
:::
  `,

  "cost-tracking-per-model": `
## 每模型成本跟踪

不同的模型价格点非常不同 — Claude Haiku 比 Opus 每 token 便宜约 30 倍,GPT-4o-mini 比 GPT-4o 便宜约 20 倍,本地模型每 token 基本无成本(只是计算)。Overview → Usage 上的每模型视图按提供商和模型细分支出,这样你可以看到钱去哪里以及支出是否匹配价值。

:::feature
**智能优化提示** color=#34d399
系统标记看起来可以在质量相似的更便宜模型上运行的运行。当高成本模型用于其他地方更便宜模型也能处理的任务模式时,提示会在成本行旁边浮现 — 指向你在 Lab 中进行 A-B 的候选 agent。
:::

### 关键点

- **按提供商和模型** — 按确切模型标识符细分成本(Sonnet 4.6、Haiku 4.5、GPT-4o、Gemini 2.5 Pro、local-ollama)
- **调用、token、成本** — 相同数据的三个视图;成本是你支付的,token 是你消耗的,调用是你调用的频率
- **每调用成本比较** — 跨模型的相同指标,这样你可以同类比较
- **优化提示** — 浮现可以降级的候选 agent;点击进入 Lab 进行 A-B 测试
- **每 agent 归因** — 深入模型行以查看哪些 agent 最多使用它

### 工作原理

Usage 视图聚合了与每 agent 视图相同的执行记录,但是在模型维度上而不是。定价在 Settings → Engine 中按模型配置,默认匹配每个提供商的公开定价;如果你有协商利率或在更便宜的端点上使用 BYOI,可以覆盖。

:::tip
每月一次,扫描按总成本排序的每模型视图。顶部条目是你最大的节省机会 — 将其放入 Lab arena 与下一个更便宜的模型对比,看看质量是否保持。大多数 agent 可以容忍模型降级;那些不能的是真正值得花费的。
:::
  `,

  "success-rate-metrics": `
## 成功率指标

每次运行以一个状态结束:成功、失败或手动审查。成功率是相对于预期行为背景下成功完成的运行的百分比。Overview → Health 选项卡和每 agent Activity 选项卡都呈现带有趋势指示器的成功率 — 周环比变化 — 这样你可以一目了然地看到可靠性是否保持。

该指标现在超越了纯粹的成功/失败。通过 **business_outcome** 跟踪,agent 本身可以声明成功运行是否产生了你实际想要的结果(销售、批准的文档、有用的摘要) — 一个与"运行是否无错误完成"分开的信号。成功率分为"干净完成"和"产生预期的业务结果" — 对于大多数 agent,第二个数字更有用。

### 关键点

- **每 agent 成功率** 带趋势箭头
- **业务结果率** — 与干净完成率分开;跟踪 agent 的工作是否实际有用
- **每触发器拆分** — 同一 agent 可能在手动运行上 99% 成功,但在计划运行上 70%;细分显示哪个触发器源有问题
- **阈值警报** — 每 agent 设置阈值;当率降到它以下时你会收到通知
- **失败原因分类** — \`timeout\`、\`model_error\`、\`tool_error\`、\`credential_expired\` 等,这样你可以看到运行 *为什么* 失败

### 工作原理

Health 选项卡在每 agent 滚动窗口上聚合运行状态。业务结果跟踪要求 agent 在其输出中发出 \`business_outcome\` 指令(大多数需要它的模板默认这样做;自定义 agent 可以显式添加)。阈值警报按 agent 配置,并通过 agent 设置的相同通知通道触发。

:::tip
在每个生产 agent 上设置 90% 阈值。警报不会告诉你 agent 失败的原因,但它会告诉你某事正在发生。Health 选项卡上的失败原因分类是你接下来进行诊断的地方。
:::
  `,

  "execution-tracing": `
## 执行追踪

追踪是 agent 做了什么的每运行逐步记录。从 Activity feed 中打开任何执行并点击 Trace 选项卡:你会按时间顺序看到每个事件 — 模型 token 流的开始和结束、每个带参数的工具调用、每个工具结果、链式 agent 中的每个决策分支、渲染的 prompt、输出。每个步骤都可展开以获取完整详情。

对于链式 pipeline,追踪跨多个 agent — lineage 画布(Events → Lineage)显示跨 agent 视图,而每运行追踪显示 agent 内部的细节。它们一起让你调试"这个 pipeline 在哪里中断?"和"agent 是逐步如何决策的?"。

### 关键点

- **按时间顺序** — 每个事件带时间戳和持续时间
- **每步骤可展开** — 点击任何步骤以查看该步骤的完整输入/输出
- **每步骤持续时间** — 查看哪个步骤慢;通常是工具调用或长模型响应
- **链式追踪** — 当 agent 由链触发时,追踪链接回上游 agent,这样你可以导航 pipeline
- **模型的逐 token** — 对于慢速流提供商有用,用户在等待

### 工作原理

每次执行在运行时将事件写入追踪存储;追踪选项卡查询该存储并渲染时间线。Token 级事件以保持追踪即使对于长响应也可用的速率采样(10k token 的响应可能捕获 500 个采样事件而不是 10k)。对于工具使用循环,模型/工具往返的每次迭代都被捕获。

:::tip
使用追踪确认模型 *实际* 收到了什么。"agent 做了奇怪的事情"错误的最大来源是模型获得了与你预期不同的输入 — 通常是因为工具结果看起来与 agent prompt 假设的不同。
:::
  `,

  "performance-trends": `
## 性能趋势

趋势是 agent 行为的长期视图 — 成功率、成本、持续时间、输出质量(在测量的地方)随时间绘制,这样你可以看到你所做更改的影响。Overview → Trends 给你图表视图;你选择 agent 和指标以及日期范围,图表呈现。

最有用的模式是"之前 vs. 之后":你在 3 月 5 日更改了 agent 的 prompt,事情变好还是变坏了?趋势视图在几秒内回答这个问题 — 你关心的线在你做出更改的日期上升或下降。

### 关键点

- **一个图表上多个指标** — 叠加成功率、成本、持续时间、业务结果率
- **多 agent 叠加** — 在一个图表上比较多个 agent 的相同指标
- **自定义日期范围** — 从"这小时"缩放到"所有时间"
- **注释** — 重要事件(prompt 版本保存、设置更改、凭证轮换)被固定到时间线,这样你可以关联
- **导出** — 如果你想做自己的分析,图表数据导出为 CSV

### 工作原理

趋势从与其余监控视图相同的执行和追踪存储聚合 — 相同数据,不同可视化。注释从版本历史和配置历史自动生成(也是持久化的),所以你不必手动标记"我在这里做了更改";系统已经知道。

:::tip
在对 agent 进行任何有意义的更改(prompt 修订、模型交换、新工具)之后,一周后查看趋势。大多数"在测试中感觉更好的" prompt 更改会产生可测量的不同指标;图表确认它(或使你的直觉无效)。
:::
  `,

  "setting-budget-limits": `
## 设置预算限制

预算限制在 agent 级别和全局级别限制 AI 支出。设置每运行预算(此单个执行不能花费超过 $X)、每天预算(此 agent 不能在所有运行中每天花费超过 $Y),或所有 agent 中的全局上限。当达到限制时,受影响的 agent 干净地暂停 — 部分运行被捕获在追踪中,没有费用持续超过上限,并触发通知。

这是无人值守 agent 最被低估的功能之一。没有预算上限的计划或 webhook 触发的 agent 可能会在 prompt 或输入做了病态的事情时一夜之间累积意外的成本。预算上限意味着最坏的情况受你提前决定的限制,而不是错误的模型运行可以做的。

### 关键点

- **每运行上限** — 单次执行的硬限制
- **每天 / 每周 / 每月上限** — 每 agent 窗口化支出限制
- **全局上限** — 所有 agent 的限制;即使每个 agent 都有自己的上限,也用作安全网
- **优雅停止** — agent 在上限处干净地停止;部分追踪被保留
- **通知** — 每次达到上限都会通知你,这样你可以决定是提高上限还是修复底层 prompt
- **软警告** — 可选的预上限阈值(例如"在 80% 时警告"),这样你知道 agent 正在朝上限前进

### 工作原理

上限在 agent 的 Settings 选项卡上配置(每运行、每窗口)或在 Settings → Engine → Budget 中(全局)。执行引擎在运行期间跟踪实时成本;当成本超过上限时,引擎通过与超时相同的路径中止运行。中止状态保留在追踪中,原因为 \`budget_exceeded\`。

:::warning
始终为任何自动触发的 agent(schedule、webhook、file watcher、chain)至少设置每天上限。没有它,病态输入或模型循环可能会在你注意到之前花费无限金额。上限是你的安全网。
:::

:::tip
从你预期典型一天成本的 3 倍开始上限。够紧以捕获失控,够松以使正常变化不会触发上限。在一周真实数据后调整。
:::
  `,

  "anomaly-detection": `
## 异常检测

异常检测将每次新运行与 agent 最近的基线进行比较,并标记看起来不寻常的运行。基线按 agent 构建:典型持续时间、典型成本、典型输出长度、典型工具调用计数。在任何这些上显著偏离的新运行都会被标记原因 — "持续时间是正常的 5 倍"、"成本峰值"、"工具调用计数异常"、"输出异常短"。

这捕获了纯粹的成功/失败指标错过的一类问题:运行完成了,但某些东西不对。Agent 花了五分钟,而它通常需要三十秒。输出是三句话,而它通常是三段话。成本翻倍,输入没有变化。这些是值得在它们成为趋势之前看到的信号。

### 关键点

- **多信号基线** — 持续时间、成本、输出大小、工具调用计数、失败率
- **每 agent 基线** — 每个 agent 都有自己的正常;对一个异常的对另一个是正常的
- **原因标记的警报** — 警报命名哪个信号偏离以及偏离了多少
- **低噪声** — 校准以呈现真正的异常值,而不是正常变化
- **与通知集成** — 异常通过 agent 配置的任何通知通道触发

### 工作原理

基线是最近运行的滚动窗口(可配置;默认 50)。每次新运行都在每个信号上评分;如果任何信号超过配置的阈值(默认距滚动平均值 3 个标准差),运行被标记并发出异常事件。异常事件出现在 Overview → Notifications 和该 agent 的 Health 选项卡上。

:::tip
你调查并解决的异常应被清除(标记为"已调查")。基线从滚动窗口中排除已调查的异常,所以系统不会漂移到将异常运行视为"正常"。
:::
  `,
};
