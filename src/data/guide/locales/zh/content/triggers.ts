export const content: Record<string, string> = {
  "how-triggers-work": `
## 触发器如何工作

触发器是 agent 的"何时"。Prompt 和工具定义 agent *做什么*;触发器定义 *何时* 以及 *用什么输入*。Personas 提供七种触发器类型:**manual**(点击按钮)、**schedule**(cron 风格)、**webhook**(入站 HTTP)、**clipboard**(复制事件匹配)、**file watcher**(文件系统事件)、**chain**(另一个 agent 的输出),以及 **event-based**(由其他 agent、插件或引擎本身发出的内部事件)。

每个 agent 可以有任意数量的触发器,跨类型混合。一个 agent 可能在每日计划上运行,响应来自 Stripe 的 webhook,在你复制邮件地址时触发,并且可以从上游 agent 链入 — 同时进行。

### 触发器类型

:::compare
**Manual**
在编辑器中点击按钮或从标题栏快速运行。每个 agent 默认都有这个。最适合测试和临时调用。
---
**Schedule**
基于 Cron。每小时、每天、每周,或带有时区的完整 cron 表达式。最适合无需输入的常规工作 — 每日摘要、每周报告。
---
**Webhook**
Agent 监听的唯一入站 URL。外部服务 POST 到它以启动 agent。最适合"对来自第三方服务的事件做出反应"。
---
**Clipboard**
当复制的文本匹配配置的模式(正则表达式、内容类型或关键字)时触发。最适合高级用户快捷方式 — 复制邮件,agent 查找它。
---
**File Watcher**
对受监视文件夹上的文件系统事件(create / modify / delete)。最适合文件在不可预测的时间到达的投放区工作流。
---
**Chain**
Agent A 的输出成为 Agent B 的输入。最适合由专注 agent 组成的多步骤 pipeline。
---
**Event-Based**
订阅内部 Personas 事件(凭证过期、插件发出事件、执行以 manual_review 完成)。最适合在你自己的设置内的反应式自动化。
:::

### 关键点

- **每个 agent 多个触发器** — 没有上限;自由组合类型
- **独立触发** — 每个触发器独立评估;一个 schedule 触发器不知道也不关心同一 agent 上的 webhook 触发器
- **每触发器过滤** — 每个触发器可以有自己的过滤条件(例如,webhook 触发器仅在 \`event_type=charge.succeeded\` 时触发)
- **触发器谱系** — Lineage 画布(Events → Live Stream → Lineage)显示哪些触发器、哪些 agent 和哪些事件是端到端连接的,跨你的整个设置
- **单独暂停** — 禁用单个触发器而不触及 agent 的其余部分

### 工作原理

触发器在 agent 的 Settings 选项卡上配置,或从 Events 页面的触发器列表中添加。执行引擎独立评估触发器条件,并在任何触发器匹配时向 agent 分派运行。运行将触发器载荷(webhook 主体、文件路径、复制的文本、上游输出、事件数据)作为输入带入 agent。

:::tip
每个 agent 都从只有一个 Manual 触发器开始。一旦你信任它的行为,一次添加一个自动触发器,这样如果出现问题,你可以隔离出哪一个引入了问题。
:::
  `,

  "manual-triggers": `
## 手动触发器

手动触发器是每个 agent 的默认设置。在编辑器中点击 \`Run\`,agent 立即启动,或使用标题栏快速运行快捷方式(在聚焦的 agent 上 \`Ctrl+Enter\`)。手动运行是你开发和测试的方式 — 它们相当于直接运行脚本以查看它在添加 cron 条目之前的作用。

你可以每次传递自定义输入。当 agent 声明它接受输入时,agent 编辑器会在 Run 按钮旁边显示一个小输入字段;你输入的任何内容都会作为触发器载荷传递。

### 关键点

- **无需配置** — 手动触发器始终可用
- **可选输入** — 直接输入,粘贴结构化 JSON,或对于不需要的 agent 不带输入运行
- **诊断运行** — 手动运行在追踪中标记为 \`manual\`,这样如果你只想查看自动活动,你可以从成本/指标报告中过滤掉它们
- **并发感知** — 手动运行尊重 agent 的并发限制;如果达到限制,点击会被拒绝并显示明确的消息

### 工作原理

手动触发器隐式存在于每个 agent 上 — 没有切换可关闭它(如果你想锁定它,请对整个 agent 使用 \`Disable\`)。引擎将手动运行视为与自动运行相同:相同的执行路径,相同的追踪捕获,相同的成本核算。唯一的区别是触发器标签。

:::tip
在 prompt 迭代期间使用手动运行。保存 prompt,运行,查看追踪,编辑。Lab arena 用于系统比较;手动用于编辑器中的快速反馈。
:::
  `,

  "schedule-triggers": `
## 计划触发器

计划触发器按重复频率运行 agent — 每小时、每个工作日早上 8 点、每月的第一个周一,或你可以编写的任何 cron 表达式。计划 UI 为常见情况提供了预设快捷方式(每小时、每天、每周),并为其他所有情况提供了原始 cron 字段。

计划尊重可配置的时区。默认情况下,agent 使用你的系统时区,但你可以按触发器覆盖 — 对于无论你在哪里都必须"东部时间上午 9 点"运行的 agent 很有用。

### 关键点

- **预设和 cron** — 从常见频率中选择或编写完整的 cron 表达式
- **每触发器时区** — IANA 时区名称(\`America/New_York\`、\`Europe/Prague\`、\`UTC\`);自动处理 DST
- **下次运行预览** — 触发器显示接下来的三个计划时间,这样你可以理智地检查你的 cron 表达式
- **暂停而不丢失** — 禁用计划触发器不会删除它;重新启用以恢复

### 设置计划

:::steps
1. **打开触发器设置** — 在 agent 的 Settings 选项卡上,或从 Events 页面;点击 \`Add trigger\` 并选择 Schedule
2. **选择预设或编写 cron** — \`0 8 * * 1-5\` 表示"工作日上午 8 点",或为常见情况使用预设
3. **设置时区** — 默认为系统;为绑定到特定业务日历的 agent 更改
4. **确认下次运行预览** — 显示三个即将到来的运行时间;验证它们是否符合你的预期
5. **保存** — 触发器立即激活并出现在 agent 的触发器列表中,带有"下次运行"倒计时
:::

:::tip
计划触发器不会回填错过的运行。如果应用关闭或机器在计划时间过去时处于睡眠状态,该运行将被跳过。对于关键任务的计划工作,运行云部署(Builder 层级),以便 orchestrator 在服务器端处理调度。
:::
  `,

  "webhook-triggers": `
## Webhook 触发器

Webhook 触发器公开 agent 监听的唯一入站 URL。当外部服务 POST 到该 URL 时,主体成为触发器载荷,agent 运行。大多数支持 webhook 的第三方服务(Stripe、GitHub、Shopify、Linear、Twilio、自定义内部 API)无需修改即可工作。

触发器支持对请求主体、标头和方法进行过滤,因此单个端点可以选择性地处理哪些事件实际启动 agent。常见模式:每个 agent 一个 webhook URL,过滤到来自上游服务的特定事件类型。

### 关键点

- **每个触发器唯一 URL** — 自动生成;从不在 agent 或触发器之间共享
- **过滤表达式** — JSONPath / 标头匹配让你只接受你关心的事件
- **重放端点** — 每个收到的 webhook 都被保留,可以从触发器详情页面手动重放
- **Send Test** — 内置按钮,将示例载荷 POST 到你的本地端点,这样你可以在没有外部服务的情况下验证过滤器和 agent 的响应
- **入站和出站是分开的** — 见下文

### 连接 webhook

:::steps
1. **添加 webhook 触发器** — Events 页面 → Add trigger → Webhook;将其绑定到 agent
2. **复制生成的 URL** — 此触发器唯一;除非你删除触发器,否则永不过期
3. **配置外部服务** — 将 URL 粘贴到服务的 webhook 配置中(Stripe Dashboard、GitHub 仓库设置等)
4. **设置过滤表达式** — 限制为特定事件类型或载荷形状,这样你不会在服务发出的每个事件上运行 agent
5. **测试** — 使用 Send Test 和示例载荷(或在上游服务中触发真实事件);检查追踪并根据需要调整过滤器
:::

### 入站 vs 出站 Webhook

Webhook 有两种风味,值得把它们分开:

- **入站 webhook(此主题)** — 外部服务调用 *你* 以启动 agent。Stripe 在充电成功时 ping 你;GitHub 在 PR 打开时 ping 你。
- **出站 webhook(单独的功能)** — *你的* agent 在完成后将其结果发送到某个通道。Personas 提供一流的出站交付到 Slack、Discord、Microsoft Teams 和通用 webhook URL,在每个 agent 的 Connectors 选项卡中配置。Agent 的输出会针对每个通道适当格式化(丰富的 Slack 块、Discord embed、Teams 卡片),并在运行完成后分派。

大多数自动化最终都同时使用两者:入站 webhook 启动 agent,agent 完成其工作,出站通道将结果传递到你团队正在观察的位置。

:::tip
对于本地开发或预生产 webhook,使用 \`Send Test\` 按钮和示例载荷,而不是配置真实的上游。你将在不绕过第三方服务的情况下更快地迭代过滤器和 prompt。
:::
  `,

  "clipboard-monitor": `
## 剪贴板监视器

剪贴板监视器监视你的系统剪贴板,并在复制的内容匹配你的规则时触发 agent。复制订单号 — agent 查找它。复制外语句子 — agent 翻译它。复制客户邮件 — agent 提取他们的账户。

匹配可以基于简单的关键字、正则表达式模式或内容类型启发式(邮件地址、URL、电话号码、JSON 形状、数字、结构化 ID)。触发器在每次剪贴板更改时评估规则,只在规则匹配时触发,所以它在后台静默存在,直到你真正复制了感兴趣的东西。

### 关键点

- **基于规则** — 每个触发器定义一个或多个规则;第一个匹配获胜
- **匹配模式** — 关键字、正则表达式或内置内容类型启发式(email/URL/phone/JSON/等)
- **默认安静** — 非匹配的复制甚至不会触发评估日志;只有匹配项会创建活动
- **输出模式** — 显示为桌面通知,推送到 Cockpit 收件箱,或保持沉默,仅写入 agent 的活动 feed
- **隐私** — 剪贴板内容保持本地;除了 agent 本身调用的任何 AI 提供商,什么都不上传

### 工作原理

触发器在应用启动时注册到 OS 剪贴板系统。当剪贴板更改时,新内容会根据此触发器上的每个规则进行评估;第一个匹配以复制的内容作为输入触发 agent。非匹配的复制被丢弃而不留下追踪,所以监视器不会膨胀活动日志。

:::tip
规则要具体。匹配每个 \`@\` 符号的剪贴板监视器将在你不想使用的复制上触发。使用完整的邮件正则,或将范围限定为"看起来像客户 ID 的复制"(匹配你自己的 ID 形状)。
:::
  `,

  "file-watcher-triggers": `
## 文件监视触发器

文件监视触发器在你指定的文件夹中文件出现、更改或消失时触发。将 CSV 拖入文件夹,agent 处理它。将图像保存到 "Process" 目录,OCR / 分类 agent 对其采取行动。修改配置文件,agent 将其与上一版本进行比较。

被监视的文件夹可以在本地文件系统上或任何同步位置(OneDrive、Dropbox、iCloud)。过滤器按文件类型/glob 模式缩小事件,这样你不会在无关的更改上运行 agent(如 macOS 的 \`.DS_Store\` 文件或临时编辑器交换文件)。

### 关键点

- **监视任何文件夹** — 本地或同步的云存储;可选的子文件夹递归
- **事件类型** — create / modify / delete;订阅一个、两个或全部三个
- **Glob 过滤器** — \`*.csv\`、\`**/invoices/*.pdf\`;支持否定模式
- **去抖动** — 连续快速修改合并为一个触发器事件(对于保存并立即保存的流程不会双重触发)
- **载荷** — agent 接收文件路径和(当文件足够小时)内联内容;否则是 agent 可以使用其文件访问工具读取的路径

### 工作原理

触发器使用 OS 原生文件监视 API(macOS 上的 FSEvents、Windows 上的 ReadDirectoryChangesW、Linux 上的 inotify)。监视器在应用打开时运行在引擎进程中。当事件匹配触发器的过滤器时,引擎以文件元数据作为输入分派 agent 运行。引擎还将文件监视器事件路由到**环境生产者**:订阅相关环境事件的任何 agent 都可以做出反应,而无需自己的监视器。

:::tip
为每个使用文件监视器的 agent 创建专用的投放区文件夹。在共享文件夹("Downloads"、"Desktop")上混合监视器会导致在保存无关文件时意外触发。
:::
  `,

  "chain-triggers": `
## 链触发器

链触发器端到端连接 agent:当 agent A 成功完成时,agent B 以 A 的输出作为其输入启动。这就是构建多步骤自动化的方式 — 每个 agent 都小而专注,链将它们缝合成一个 pipeline。

链可以分支(一个 agent 的输出馈送多个下游 agent)和收敛(多个上游 agent 馈送到一个下游)。它们也可以是条件性的 — 触发器可以有一个过滤器,只转发匹配条件的输出,所以你只在重要的情况下运行下游 agent。

:::diagram
[Research Agent] --> [Writing Agent] --> [Formatting Agent] --> [Final Output]
:::

### 关键点

- **输出 → 输入连线** — 自动;下游 agent 的 prompt 看到上游输出的原样(如果你配置了变换器,则进行变换)
- **分支和收敛** — 支持多对一和一对多链
- **条件转发** — 链触发器上的过滤表达式让你只在某些条件下转发(输出包含 "error",或字段超过阈值)
- **故障停止链** — 如果上游 agent 失败,下游链接的 agent 不会运行;故障在 lineage 视图中浮现,这样你可以确切看到链在哪里断裂
- **端到端可见** — Events → Live Stream → Lineage 画布显示链接 agent 的完整图和实时执行流

### 工作原理

在下游 agent 的 Settings 选项卡上,添加 Chain 触发器并选择上游 agent。引擎将下游 agent 订阅到上游的完成事件;当上游发出"执行完成且成功"时,引擎将输出作为输入转发给下游。条件过滤器在服务器端在调度下游运行之前评估。

:::tip
链中的每个 agent 应该做一件事并且做好。三个小而专注的 agent 的链比一个大的做所有事情的 agent 容易调试得多 — 你可以在 lineage 视图中看到哪个阶段失败了,你可以在不触及链的其余部分的情况下将 agent 换成更好的版本。
:::
  `,

  "event-based-triggers": `
## 基于事件的触发器

基于事件的触发器将 agent 订阅到内部 Personas 事件。应用中发出事件的任何内容 — 另一个 agent 完成、凭证过期、插件触发(如 Drive 插件在 Local Drive 中文件更改时发出 \`drive.document.*\` 事件)或引擎本身标记 manual-review 情况 — 都可以驱动订阅的 agent。

这是最灵活的触发器类型。与 webhook(来自外部系统)或计划(按时钟触发)不同,事件来自你自己的 Personas 设置内部。构建事件驱动的设置,其中一个信号可以扇出到多个 agent 而无需显式连线。

### 关键点

- **订阅任何事件** — agent 完成事件、插件事件、引擎事件、其他 agent 发出的自定义事件
- **载荷感知** — 每个事件携带数据(agent 的输出、文件路径、凭证 ID);订阅的 agent 将其作为输入接收
- **一对多** — 多个 agent 可以订阅同一事件,触发时全部并行运行
- **过滤表达式** — 按载荷字段限制(仅在 \`severity = critical\` 的事件上触发)
- **可发现** — 事件注册表可在 Events 页面浏览;你可以准确看到可用的事件以及它们携带的字段

### 工作原理

向下游 agent 添加 Event 触发器并从注册表中选择事件。引擎在启动时订阅 agent,并在匹配事件触发时以事件载荷分派运行。从 agent 的角度看,插件发出的事件与引擎发出的事件看起来相同 — 它们都通过同一总线流动。

:::tip
基于事件的触发器是你在不改变 X 的情况下构建"如果 X 那么也 Y"关系的方式。在新 agent 上添加事件触发器,将其指向另一个 agent 发出的事件,新行为就会反应式发生 — 现有 agent 不知道也不关心。
:::
  `,

  "combining-multiple-triggers": `
## 组合多个触发器

一个 agent 可以有任意数量的任何类型的触发器。大多数生产 agent 至少有两个:一个手动触发器(用于测试和临时调用)加上一个或多个自动触发器(schedule、webhook、chain、event)。常见的是看到一个 agent 具有 schedule + webhook + chain 组合 — 同一个 agent 可以作为每日批处理的一部分运行,响应实时 webhook,并作为链式 pipeline 中的一步。

多个触发器互不干扰。每个触发器按自己的计划或事件触发;如果两个在同一瞬间触发,agent 运行两次(并发限制允许)。每次运行的追踪都捕获是哪个触发器启动的。

### 关键点

- **没有上限** — 一个 agent 可以有几十个触发器
- **独立评估** — 每个触发器独立评估和分派
- **每触发器过滤和配置** — schedule 有自己的 cron,webhook 有自己的 URL 等
- **追踪中的触发器标签** — 每次运行都标记了启动它的触发器,这样你可以按触发器源过滤活动
- **选择性禁用** — 禁用单个触发器而不触及其余部分

### 工作原理

Agent 上的 Settings → Triggers 选项卡显示每个附加的触发器、其状态(启用/禁用)及其上次触发时间。使用 \`Add trigger\` 添加新的;同一选择器让你创建七种触发器类型中的任何一种。禁用的触发器留在列表中,这样你可以稍后重新启用它们,无需重新配置。

:::tip
一个有用的模式:始终保持 Manual 触发器活跃(用于调试),并将每个"真实的"自动触发器与采用相同输入形状的兄弟 Manual 触发器配对。这样你可以在任何时候手动重放任何自动化载荷以进行调查。
:::
  `,

  "testing-and-debugging-triggers": `
## 测试和调试触发器

Events → Test 选项卡是触发器测试器。对于任何触发器,你可以发送示例载荷(webhook 主体、文件事件、剪贴板字符串、事件数据),并准确看到 agent 会收到什么以及它如何响应 — 无需外部服务或等待实际触发器时间。

对于已经触发但 agent 没有按你预期运行的触发器,触发器日志显示每次评估:匹配的过滤器、被拒绝的过滤器、载荷形状、分派时间。Lineage 画布(Events → Live Stream → Lineage)是视觉等价物 — 它显示跨你整个设置的实时触发器评估和分派。

### 关键点

- **模拟任何触发器** — 粘贴载荷并查看 agent 的响应
- **触发器日志** — 每次触发尝试都被记录,包括过滤器拒绝,这样你可以看到什么不匹配
- **Lineage 画布** — 触发器、agent 和事件的可视化图,当事情触发时带有实时流指示器
- **Webhook 的 Send Test** — 内置按钮,将示例主体 POST 到本地端点
- **重放** — 过去的触发器触发可以使用确切的原始载荷重放,对于"如果此 Stripe webhook 再次命中 agent 会发生什么"很有用

### 逐步调试触发器

:::steps
1. **确认触发器已启用** — agent 上的 Settings → Triggers 选项卡;静音图标表示触发器已禁用
2. **检查触发器日志** — Events → Test → Logs,按你的触发器过滤;查找没有分派的评估
3. **针对载荷检查过滤器** — 如果触发器评估了但没有分派,过滤表达式正在拒绝它;复制载荷并显式测试过滤器
4. **验证分派到达 agent** — 执行追踪应显示触发器标签;如果没有执行出现,触发器从未分派(过滤器问题、并发限制或已禁用的 agent)
5. **使用 lineage 画布** — 对于链或事件触发器,打开 Lineage 并追溯路径;你会看到流在哪里中断
:::

:::tip
"我的触发器不触发"几乎总是意味着以下之一:触发器被禁用,过滤器太严格,agent 被禁用,或外部服务实际上没有发送你认为它在发送的内容。触发器日志在一分钟内区分所有四种情况。
:::
  `,
};
