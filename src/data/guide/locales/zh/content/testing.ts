export const content: Record<string, string> = {
  "why-test-your-agents": `
## 为什么要测试你的 agent?

测试是在迭代过程中保持 agent 值得信赖的方式。每次 prompt 编辑、每次模型切换、每次添加新工具都会以你无法从阅读 diff 完全预测的方式改变 agent 的行为。测试将这种不确定性转化为证据:针对代表性输入运行新版本,与上一版本进行比较,查看你是否改善了想要改善的事情,而没有回归你不想回归的事情。

每个 agent 编辑器上的 Lab 选项卡是这件事发生的地方。它有四种模式 — Arena、A-B、Matrix、Eval — 每种回答不同的问题。Arena 比较同一 prompt 上的模型。A-B 比较同一模型上的两个 prompt。Matrix 测试 prompt 组件的组合。Eval 是完整网格:每个 prompt × 每个模型。

### 关键点

- **及早捕获回归** — 每次更改后测试是你避免"agent 以前能工作,我搞坏了什么?"的方式
- **系统地比较备选方案** — Arena 和 A-B 让你以证据而不是直觉在选项之间做出选择
- **生成适应性数据** — Lab 运行积累每 prompt 分数,馈送 genome evolution(Builder 层级)
- **可重用的输入集** — 测试输入按 agent 保存;相同 prompt,相同数据,可重复比较

### 工作原理

每种 Lab 模式将相同的触发器载荷并行分派给多个 agent 变体(不同的 prompt、不同的模型,或两者)。输出与定量元数据(持续时间、成本、token 计数)和你的主观评分按钮并排显示。结果落入 agent 的测试历史并向前馈送到适应性评分。

:::tip
捕获 prompt 回归最便宜的时刻是在你刚刚编写它之后。让 Lab → A-B 与上一个 prompt 版本对比成为你在每次 prompt 编辑时的习惯;摩擦比三天后在生产运行中发现回归低得多。
:::
  `,

  "the-testing-lab-overview": `
## 测试实验室概览

每个 agent 编辑器上的 Lab 选项卡是一个工作区,有四种模式。按你想了解的内容选择模式:

### 四种模式

:::compare
**Arena**
相同 prompt,多个模型。将一个输入并行发送到 Claude / GPT / Gemini / 本地。最适合"哪个模型适合此 agent?"
---
**A-B**
两个 prompt,相同模型。在相同条件下比较 prompt 更改与其前身。最适合"此编辑是否改善了事情?"
---
**Matrix**
组合。定义 prompt 组件,矩阵测试每个组合(3 × 4 = 12 个变体)。最适合"我有多个竞争想法 — 哪个组合获胜?"
---
**Eval**
完整网格:N 个 prompt × M 个模型。当你想一起优化 prompt *和* 模型时的完整图景。最适合当桌面上有重大更改时。
:::

### 工作原理

每种模式共享相同的输入选择器(手动输入、结构化 JSON 粘贴,或从此 agent 的历史中重放真实的过去执行)和相同的评分 UI。输出列展开为完整追踪(模型调用、工具调用、决策分支),就像常规执行一样。结果保存到测试历史,标记了测试模式,这样你可以按模式浏览过去的测试。

对于链式 agent,Lab 只测试此 agent — 使用你指定的输入模拟上游,这样你可以迭代 pipeline 的一个阶段,而无需重新运行整个链。

:::tip
大多数周,Arena 和 A-B 就足够了。Matrix 用于"我有三个似乎合理的重构,想要比较",Eval 用于"我正在考虑重大重写或层级更改"。不要默认使用重型模式 — 更便宜的通常就足够了。
:::
  `,

  "arena-testing": `
## Arena 测试

Arena 将相同的 prompt 和相同的输入并行发送到多个模型,然后将结果并排呈现。成本和持续时间显示在输出旁边,所以你在三个轴上比较 — 质量(你的判断)、速度(引擎测量)和成本(逐 token)。

最常见的用途是模型选择决策:"此 agent 一直在 Sonnet 4.6 上运行,Haiku 4.5 会以 1/30 的成本保持质量吗?"Arena 在一次测试中回答这个问题,而不是数周的生产观察。

### 关键点

- **并行分派** — 所有模型同时运行;总挂钟时间 = 最慢的那个,而不是总和
- **并排输出** — 每个模型的完整输出可见,无需切换选项卡
- **显示成本 + 持续时间** — 在每个输出下方,与文本在同一视图中
- **每列评分 UI** — 每个模型的赞 / 踩 / 星;评分持久化到 agent 的适应性数据中
- **从历史中重放** — Arena 测试可以从此 agent 的任何过去执行中提取输入,所以你在真实形状上测试

### 工作原理

Arena 使用 agent 的当前 prompt 和工具配置为每个选定的模型分派一个执行。每个执行都是独立的(独立追踪、独立成本核算),并标记为 \`arena\`,这样它不计入 agent 的正常生产指标。结果显示为列;你对每列评分;评分馈送到此 agent 的每模型适应性数据中。

:::tip
每次 Arena 运行最多选择 3 个模型。超过这个数量,并排阅读就变得难以管理。如果你正在考虑 5+ 个模型,运行多个 Arena 对比并记下哪个模型赢得了每一轮。
:::
  `,

  "ab-testing-prompts": `
## A-B 测试 prompt

A-B 在相同模型上对相同输入运行两个 prompt 变体,所以唯一的变量是 prompt。这是评估 prompt 编辑的正确工具:加载先前版本为 A,新版本为 B,在代表性输入上运行,查看哪个产生你想要的结果。

Lab 的版本选择器与 prompt 的版本历史集成 — 你不需要复制粘贴旧版本,只需从下拉菜单中选择它。这使"将我的当前草稿与上周的工作版本进行比较"成为一键设置。

### 关键点

- **两个 prompt,一个模型,一个输入** — 单变量比较
- **从版本历史中选择** — A 或 B 可以是此 agent prompt 的任何过去版本
- **相同的追踪保真度** — 两个变体都获得完整的执行追踪,所以你可以比较工具调用模式,而不仅仅是最终输出
- **多个输入轮次** — 按顺序对多个不同输入运行 A-B 以测试泛化,而不仅仅是一个幸运情况
- **分数持久化到适应性中** — A-B 评分馈送 Arena 和 genome 使用的相同适应性数据

### 工作原理

A-B 引擎将两个 prompt 作为独立执行分派,并在结果面板中标记为 A 和 B。除此之外,它们是常规执行 — 相同的追踪、相同的成本核算,但标记为 \`ab_test\`,这样它们可以在测试历史中过滤,不会污染生产指标。

:::code-compare
### Version A
Summarize the document.
Keep it short.
---
### Version B
Summarize the document in exactly
3 bullet points. Each bullet should
be one sentence. Start with the
most important finding.
:::

:::warning
每次 A-B 轮次只更改一件事。如果 B 与 A 在 *格式*、*语气* *和* 长度上都不同,你无法判断是哪个维度导致了分数变化。做一个更改,运行 A-B,接受或拒绝,然后做下一个更改。
:::
  `,

  "matrix-testing": `
## Matrix 测试

Matrix 是一次性的组合 A-B-C-D-…。你将 prompt 定义为组件(例如,intro × instructions × output-format),矩阵生成每个组合,分派它们全部,并按适应性分数对结果进行排名。

3 个组件 × 3 个选项 = 27 个组合 — 远远超过你手动测试的数量,但引擎可以轻松并行扇出。当你对如何构造 prompt 有多个竞争想法,并希望找到实际表现最佳的组合而不是你猜测的组合时,Matrix 最有用。

### 关键点

- **定义组件,获得组合** — 矩阵将组件展开为所有有效组合
- **并行分派** — 每个组合同时运行(受提供商速率限制约束)
- **排名结果** — 适应性评分网格,从最佳到最差排序
- **组件级归因** — 查看哪些组件与高分相关;即使你没有逐字采用最佳获胜者也很有用
- **保存获胜组合** — 一键将获胜组合设置为 agent 的活跃 prompt

### 工作原理

你在矩阵选项卡中将每个组件定义为标记的变体集。引擎将每个组合构造为可渲染的 prompt,并将每个作为独立执行分派。结果聚合到按你选择的适应性信号(评分、性价比、速度、自定义)排名的网格中。组件级归因通过对共享该组件的组合的平均适应性来计算 — 所以即使没有单个获胜者脱颖而出,你也能了解平均而言哪种 intro / 指令风格 / 输出格式表现最佳。

:::info
3 个组件 × 3 个选项 = 27 个变体。4 × 4 = 256。矩阵可以处理大型网格,但你会按比例消耗 token。从 3 × 3 开始,只有当结果真正不明确时才扩展。
:::

:::tip
Matrix 在 prompt 重大重新设计后最有用。当你不确定新结构是否比旧结构更好时,对几个代表性输入对 3-4 个候选结构进行矩阵测试 — 获胜者通常比你预期的更清晰。
:::
  `,

  "eval-testing": `
## Eval 测试

Eval 是完整网格:每个 prompt 变体 × 每个模型。你选择 prompt(通常 2-3 个候选)、选择模型(通常 2-4 个),eval 网格运行所有组合并呈现分数的热图。最佳的 prompt-模型对被突出显示。

这是重量级模式 — token 上最昂贵,覆盖最彻底。当你做出影响两个轴的重大决策时使用它:"我们正在考虑重写 prompt 并迁移到更便宜的模型,我们可以同时做这两件事并仍然达到质量标准吗?"

### 关键点

- **N 个 prompt × M 个模型** — 跨两个维度的分数热图
- **最佳组合突出显示** — 适应性排名,最优单元格在视觉上被标出
- **每轴细分** — 查看是 prompt 更改还是模型更改驱动了分数变化
- **测试历史标记** — eval 运行在历史中以 \`eval\` 标签存放以供日后审查
- **一键采用** — 将最佳组合(prompt 版本 + 模型选择)应用到实时 agent

### 工作原理

Eval 并行分派 \`prompts × models\` 次执行(受提供商速率限制约束)。每个单元格都是一个独立的执行,带有自己的追踪。网格视图按 prompt-模型对聚合;你使用与 Arena 和 A-B 相同的 UI 对单元格评分;适应性分数汇总为每单元格排名。顶部单元格是推荐的组合 — 直接从网格视图中采用。

:::warning
Eval 是最昂贵的模式。3 个 prompt × 4 个模型 × 5 个输入 = 60 次执行,每次都有自己的模型调用。谨慎运行,在代表性输入集上,只有当决策真正跨越两个轴时。对于仅 prompt 的决策,使用 A-B;对于仅模型的决策,使用 Arena。
:::
  `,

  "rating-and-scoring-results": `
## 评分和打分结果

在任何 Lab 测试之后,每个输出行都有评分控件:二元判断的赞 / 踩,或细微情况的 1-5 星刻度。你的评分馈送两件事:agent 的每变体适应性分数(用于矩阵和 eval 中的排名,并作为 Builder 层级上 genome-evolution 的选择压力)和你随着时间推移在所有测试中的个人偏好信号。

评分是个人的 — 它们编码了你对质量的判断,而不是客观指标。这是有意的;你是知道 agent 输出是否匹配你需求的人,这就是系统优化的信号。

### 关键点

- **二元或 1-5 星** — 选择你愿意保持一致的任何刻度
- **每输出评分** — 每个测试输出都有自己一行的评分控件;在你评分之前,不会自动聚合任何内容
- **驱动适应性分数** — 评分馈送 Matrix / Eval / genome 使用的每变体适应性信号
- **反馈历史持久化** — 你曾经给出的每个评分都被存储;对"我在过去的测试中是否将 X 评得比 Y 高?"很有用
- **一致性比精确性更重要** — 你可以一致给出的 4 星比你给一次然后再也不给的 5 星更有用

### 工作原理

评分存储在特定执行(追踪、prompt 版本、模型、输入)上。适应性聚合器读取评分 + 客观指标(成本、持续时间、成功)并计算每变体适应性分数,该分数用于排名。Genome evolution(Builder 层级)使用评分作为选择父 prompt 进行繁殖的主要选择压力。

:::tip
基于你实际想要的内容评分,而不是技术上令人印象深刻的内容。简短正确的答案通常胜过长而精心制作的答案。系统针对你的偏好进行优化,所以诚实、一致的评分产生针对 *你的* 判断调整的 agent。
:::
  `,

  "genome-evolution-basics": `
## Genome Evolution 基础

Genome evolution(Builder 层级)从你最佳评分的过去测试中自动繁殖新的 prompt 变体。每个"代"变异和重组上一代的最佳表现 prompt;经过几代,prompt 收敛到比你的起始点持续得分更高的配置。它是以你的评分作为适应性函数的进化搜索。

一旦开始,该过程就无人值守。你提供起始 prompt 和适应性信号(通常是你的评分历史加上可选的客观指标,如成本或持续时间),设置种群规模和代数,然后让它运行。Agent 的正常触发器在进化期间保持暂停,以保持比较干净。

:::info
Genome evolution 一旦启动就无人值守。你设置参数,引擎创建变体,针对你的输入集测试它们,按你的评分对它们评分,并将获胜者重组到下一代。你审查最终种群并手动采用获胜者 — 系统永远不会静默地更改你的实时 prompt。
:::

### 关键点

- **自动变异 + 选择** — 引擎生成顶级父代的变异并通过适应性进行选择
- **代数 + 种群** — 典型配置是 5-10 代,每代 8-12 个变体
- **适应性函数 = 你的评分** — 主要信号;次要信号(成本、持续时间)是可配置的权重
- **所有代都版本化** — 每个生成的 prompt 都保存在 agent 的版本历史中;什么都不丢失
- **手动采用** — 引擎永远不会静默地交换你的实时 prompt;你审查并采用获胜者

### 工作原理

每一代从父种群开始。引擎通过小型结构化变异(改述、重新排序部分、调整示例等)和交叉(组合两个父代的段)生成子变体。每个子代针对你的输入集运行;评分产生适应性分数;得分最高的子代成为下一代的父种群。在配置的代数后,你会看到最终排名的种群,并可以采用任何变体。

### 实战示例

:::usecases
**邮件分类调整**
当前 prompt 误分类 15% 的邮件
---
运行 5 代,每代 10 个种群。最终得到一个误分类 3% 的变体 — 一键采用。
===
**格式一致性**
Agent 在不同输入形状上的输出格式不一致
---
Genome 在多样化的输入集上以格式一致性作为适应性信号进化;输出趋于稳定。
===
**在不损失质量的情况下降低成本**
你想找到一个仍然产生良好输出的更精简 prompt
---
将每 token 成本以负权重添加到适应性函数;进化找到保持评分的更短 prompt。
:::

:::info
进化期间创建的每个变体都在 agent 的 prompt 历史中版本化。如果采用的变体 N+1 在生产中表现不佳,恢复变体 N 只需一次点击 — 不会丢失工作。
:::

:::tip
耐心是有回报的。第 1 代通常不会比你的起始 prompt 显著更好 — 变异很小,许多是哑弹。到第 3-4 代时,幸存的种群集中在实际的改进上;那通常是你会看到明确获胜者的时候。
:::
  `,

  "running-a-breeding-cycle": `
## 运行繁殖周期

"繁殖周期"是一次完整的进化运行:选择 agent、设置参数、启动、等待、审查种群、采用。每个周期是 N 代,每代 M 个变体,针对你选择的输入集进行测试。总成本大致为 \`generations × population × input-count × per-run-cost\` — 从参数可预测。

Lab 上的 Genome 选项卡是入口点。它以为代表性起点调整的默认参数(5 代 × 10 个变体 × 5 个输入)打开,这足以看到有意义的变化而不会消耗过多的 token。在启动之前调整参数,如果你想要更重或更轻的周期。

:::steps
1. **在你想进化的 agent 上打开 Lab → Genome**
2. **选择输入集** — 手动输入,保存的集合,或从历史中重放
3. **配置适应性权重** — 评分权重(主要)、成本权重(如果你想要更短,则为负)、持续时间权重(如果你想要更快,则为负)
4. **设置代数和种群** — 5 × 10 是默认值;对于更难的问题提高两者,对于快速实验降低两者
5. **点击 Start Cycle** — 引擎无人值守运行;你可以让应用保持打开或稍后回来
6. **审查最终种群** — 按适应性排名,每个变体的追踪可用
7. **采用获胜者** — 或你喜欢的任何其他变体;agent 的活跃 prompt 被更新,周期的完整种群在版本历史中保留
:::

### 工作原理

每一代并行运行:引擎跨输入集同时分派所有 M 个变体(受提供商速率限制约束),收集结果,通过适应性函数对它们评分,选择顶级表现者作为父代,为下一代生成子代,并继续。进度 UI 显示每代实时最佳和平均适应性,这样你可以看到种群是否在改进。

:::tip
从小输入集(3-5 个代表性案例)和默认的 5 × 10 周期开始。如果结果明显改善,你完成了。如果含糊不清,扩展输入集并从上一个获胜者开始另一个周期。迭代周期通常胜过一个巨大的周期。
:::
  `,

  "adopting-evolved-prompts": `
## 采用进化的 prompt

繁殖周期结束时,你会看到按适应性排名的最终种群,顶级变体突出显示。采用是一键 — 变体成为 agent 的活跃 prompt,先前的活跃 prompt 保留在版本历史中(所以回滚也是一键),周期的完整种群也保留,以防你以后想采用不同的变体。

采用操作运行与任何其他 prompt 更改相同的预检查:setup-status 验证 agent 的凭证和工具仍然有效,版本在历史中检查点保存,如果 agent 有计划触发器,下一个计划运行自动使用采用的变体。

### 关键点

- **一键采用** 来自排名种群视图
- **先前版本保留** 在历史中;恢复也是一键
- **完整种群保留** — 周期中的任何变体以后仍可采用
- **预检查运行** — setup-status 验证、凭证验证、触发器兼容性
- **实时触发器自动使用新变体** — 无需单独的"部署"步骤

### 如何采用

:::steps
1. **等待繁殖周期完成** — 通常 10-30 分钟,取决于参数
2. **打开最终种群视图** — 按适应性排名的变体,每个变体的追踪可访问
3. **阅读顶级变体的 prompt** — 快速理智检查意外措辞或奇怪的变异
4. **可选地检查第二/第三名变体** — 有时稍低的适应性带有更短/更清晰的 prompt
5. **点击你选择的 Adopt**;预检查运行;agent 的活跃 prompt 原子更新
6. **验证下一次实时运行** — 通常使用代表性输入的手动运行是最便宜的确认采用的变体如测试分数承诺的那样运行
:::

:::tip
在点击 Adopt 之前阅读采用的变体。进化找到高适应性 prompt,但偶尔变体通过利用输入集的某个怪癖得分良好;阅读 prompt 是捕获"这也会通过我的测试但很奇怪"的安全检查。
:::
  `,

  "fitness-scoring-explained": `
## 适应性评分解释

适应性是驱动 Matrix / Eval / Genome 选择的单一数字。它将你的手动评分(主要信号)与客观指标(成本、持续时间、成功率、输出长度目标一致性、自定义信号)组合成加权分数。你按 agent 或按测试配置权重 — 默认情况下,评分占主导,客观指标是平局打破者。

分数按每变体每输入计算,然后跨测试集中的所有输入聚合以产生每变体一个适应性。变体按聚合适应性排名;该排名是 genome 选择算法消耗的,也是 Lab UI 用于突出获胜者的。

### 关键点

- **每变体单一聚合分数** — 通常 0.0–1.0 或 0–100,取决于显示偏好
- **多个输入源** — 评分(主要)、成本、持续时间、成功、输出格式一致性、自定义适应性函数
- **每 agent 权重** — 强调重要的;对于成本敏感的 agent,加重成本;对于质量敏感的,加重评分
- **跨输入聚合** — 变体在每个输入上评分然后平均,所以在一个输入上出色而在另一个上损坏的变体得分低于稳定的平庸的
- **透明细分** — 点击任何适应性数字以查看每信号贡献

### 工作原理

适应性聚合器读取执行结果(成本、持续时间、成功)、评分历史(每执行)以及为 agent 注册的任何自定义适应性信号。每个都归一化到 0-1 范围,乘以其配置的权重,并相加。结果是变体的适应性;跨测试集中所有输入的聚合是显示的分数。

:::tip
默认权重(90% 评分,10% 成本)针对大多数 agent 调整。如果你发现自己不同意系统在 eval / 矩阵测试中的"获胜者",最有用的调整通常是进一步提高评分权重(95%),这样系统更信任你的判断。对于 token 成本是真正问题的非常高吞吐量 agent,提高成本权重。
:::
  `,

  "test-history-and-trends": `
## 测试历史和趋势

你运行的每个 Lab 测试都保留在 agent 的测试历史中。历史视图(Lab → History)显示按日期排序的过去测试,带有模式标签、输入集、适应性分数和最终结果(已采用 / 已拒绝 / 已替换)。点击任何过去的测试以在其原始模式中重新打开它以重新审查,或将参数克隆到新测试中。

Trends 子选项卡随时间绘制 agent 级指标 — 当前活跃 prompt 的适应性、每运行成本、每运行持续时间、业务结果率。该图带有重要事件(prompt 更改、模型交换、触发器添加)的注释,这样你可以查看每次更改对实时 agent 指标的影响。

### 关键点

- **保留每个测试** — 完整的输入、输出、评分、适应性;没有什么是垃圾回收的
- **模式标记** — 按 Arena / A-B / Matrix / Eval / Genome 过滤以找到特定的过去测试
- **趋势图** 带有每个有意义变化点的自动注释
- **比较过去测试与当前状态** — 对"当前 prompt 是否仍比我三周前拒绝的那个更好?"有用
- **可导出** — 测试历史导出为 CSV 以进行外部分析

### 工作原理

测试结果存储在与生产运行相同的执行存储中,带有测试模式标签以供过滤。Trends 视图从此存储聚合;自动注释从版本历史和配置历史(也持久化)中提取。历史中没有任何东西是可变的 — 过去的测试是测试时所测内容的不可变记录。

:::tip
Trends 视图是回答"我的 agent 真的随时间变得更好吗?"的最佳单一地方。每月打开一次;如果适应性趋势平坦或下降,最近的更改没有帮助,是时候思考而不是发货更多更改了。
:::
  `,
};
