export const content: Record<string, string> = {
  "what-are-pipelines": `
## 什么是 pipeline?

Pipeline 是一个协调的 agent 组,它们在彼此之间传递工作以处理多步骤任务。你不是构建一个大而全能的 agent,而是构建小而专注的 agent 并将它们连接起来 — 每个都是专家,pipeline 处理编排。侧边栏中的 Pipeline 部分是 pipeline 存放的地方;其中的 Team Canvas 是你组合它们的地方。

Personas 中的 pipeline 是一等公民 — 它们有自己的执行历史、自己的可观察性表面、自己的团队 memory(所有 pipeline 中的 agent 都可以读取的共享上下文),它们可以像单个 agent 一样被触发(schedule、webhook、manual、chain)。区别在于一个触发器触发整个 pipeline,而不是一个 agent。

:::compare
**Single Agent**
一个 prompt、一个工具集、一个输出。设置简单;当任务自然分解为阶段时受限。
---
**Pipeline** [recommended for multi-stage work]
几个专注的 agent,连接到一个流中。每个 agent 都小且易于调试;pipeline 将它们组合成更大的能力。共享团队 memory 让 agent 传递结构化上下文,而不仅仅是文本。在团队画布上端到端可见。
:::

### 关键点

- **多 agent 流** — agent 沿着定义的连接将输出传递到输入
- **团队 memory** — 所有 pipeline agent 都可以读写的共享上下文存储,与每 agent memory 分开
- **可视化编辑器** — Team Canvas;放置 agent、绘制连接、配置路由
- **可重用** — 同一 pipeline 为任何匹配的触发器载荷运行;pipeline 也是可克隆的
- **可观察** — 完整的 pipeline 级别执行历史,带有每 agent 细分

### 工作原理

你在 Team Canvas 上组合 pipeline:放置 agent、绘制连接、根据需要配置条件分支。当 pipeline 运行时,数据沿着连接流动 — 每个 agent 的输出成为画布连线的下游 agent 的输入。引擎端到端跟踪运行,所以你看到的是一次 pipeline 执行,而不是 N 个不相关的 agent 运行。

### 实战示例

:::usecases
**DevOps 自动化**
在 GitHub 上打开 pull request
---
PR Reviewer agent 分析 diff,Test Runner 验证构建,Release Notes 起草更新日志,Slack Notifier 将摘要发布到你的团队频道 — 由 GitHub webhook 触发的单个 pipeline。
===
**内容工作流**
你需要从一个主题发布一篇博客文章
---
Research agent 收集来源,Writer 起草作品,Editor 润色,Publisher 为你的 CMS 格式化 — pipeline 管理交接,团队 memory 携带共享的风格指南。
===
**客户支持分类**
新工单到达
---
Classifier 确定紧急程度和类别,Knowledge agent 检索相关文档,Drafter 编写候选回复,如果置信度低,Router 升级到人工。
:::

:::info
没有 pipeline 大小的硬性上限。从两个 agent 开始以验证数据流,通过一次添加一个专家来增长。具有 10+ 个 agent 的 pipeline 与小型 pipeline 一样可靠地工作;引擎以相同的方式处理编排。
:::

:::tip
将 pipeline 中的每个 agent 视为单一用途的函数:一种特定的输入形状,一种特定的输出形状。每个 agent 越小越专注,整个 pipeline 越容易调试,各个部分在 pipeline 中越可重用。
:::
  `,

  "the-team-canvas": `
## Team Canvas

Team Canvas 是 pipeline 的可视化编辑器。打开 Pipeline → Team Canvas,你会看到你的 pipeline 作为图:由有向边连接的 agent 节点。从左侧的库面板放置 agent,通过从 agent 的输出端口拖动到另一个 agent 的输入端口来绘制连接,使用条件节点配置分支。画布支持平移、缩放、多选、自动布局和键盘导航。

画布不仅仅是可视化 — 它是编辑器。你在画布上做的每个更改(放置 agent、绘制连接、添加条件节点)都会立即更新 pipeline 的定义。保存以提交;pipeline 以与 agent prompt 相同的方式版本控制。

### 关键点

- **拖放** agent 从库到画布
- **连接绘制** — 从输出端口点击拖动到输入端口;数据在运行时沿连接流动
- **条件节点** — 在 agent 之间添加路由节点以基于数据分支
- **自动布局** — 一键将画布整理成从左到右或从上到下的流
- **版本化** — 画布快照与 pipeline 一起保存;恢复以前的布局和拓扑

### 构建你的第一个 pipeline

:::steps
1. **打开 Pipeline → Team Canvas** — 侧边栏 → Pipeline → New Pipeline(或打开现有的)
2. **浏览 agent 库** — 左侧面板;按组过滤或搜索
3. **将 agent 拖到画布上** — 按执行顺序大致放置它们
4. **绘制连接** — 输出端口(右边缘)到输入端口(左边缘)
5. **如果需要添加条件节点** — 工具栏 → Conditional;配置分支
6. **保存** — Ctrl+S;pipeline 立即提交并可运行
:::

:::tip
从左到右从上到下是最易读的约定。一旦拓扑设置好,使用自动布局(工具栏按钮);它产生一个干净的视觉流,帮助任何阅读画布的人 — 包括未来的你 — 一目了然地理解 pipeline。
:::
  `,

  "adding-agents-to-a-pipeline": `
## 向 pipeline 添加 agent

Agent 从 Team Canvas 左侧的库面板添加到 pipeline。将任何 agent 拖到画布上以放置它;agent 的默认设置会传递过来(prompt、工具、模型、凭证),但如果你希望此 agent 在这里的行为与其他地方略有不同,可以按 pipeline 覆盖。

同一个 agent 可以参与多个 pipeline,每个都有自己的覆盖设置。对底层 agent 的更改(例如,agent 自己的编辑器中的 prompt 修订)传播到使用它的所有 pipeline;按 pipeline 的覆盖不会,它们只存在于 pipeline 中。

### 关键点

- **从库拖动** — 你创建的任何 agent 都可用
- **按 pipeline 覆盖** — 输入映射、输出变换器、模型偏好(如果你希望此 pipeline 在此阶段使用更便宜的模型)、故障转移提供商
- **多 pipeline 重用** — pipeline A 和 pipeline B 中的 agent 在每个 pipeline 中具有独立的覆盖集
- **底层 agent 更改传播** — prompt 编辑、工具更改等流向每个使用该 agent 的 pipeline(按 pipeline 的覆盖不会)
- **就地替换 agent** — 右键单击 → Replace;如果输入/输出形状匹配,新 agent 继承旧 agent 的连接

### 工作原理

在画布上放置 agent 会创建一个对该 agent 的 *pipeline 范围引用*。引用包括覆盖集(任何按 pipeline 的自定义)和画布上的位置。在运行时,引擎解析引用,在 agent 基础配置之上应用覆盖,并分派运行。

:::tip
抵制将沉重的按 pipeline 自定义烘焙到覆盖集中的诱惑。如果你发现自己在一个 pipeline 中覆盖很多东西,通常更干净的做法是克隆 agent(给克隆一个清晰的名字,如 "Email Writer - Pipeline B")并使用克隆 — 保持按 pipeline 的自定义显式而不是隐藏在覆盖面板中。
:::
  `,

  "connecting-agents-with-data-flow": `
## 用数据流连接 agent

画布上的连接是从 agent 的输出端口到另一个 agent 的输入端口的有向边。每个连接将上游 agent 的输出作为输入传递给下游 agent — 默认逐字传递,或由内联变换器(在传递之前重塑输出的小表达式)变换。

连接是可配置的:你可以添加变换器、标记它们(在复杂的 pipeline 中很有用),并暂时关闭它们以进行调试而不删除它们。多个连接可以从一个输出扇出(广播:下游 agent 都接收相同的数据),或扇入一个输入(引擎将来自多个上游 agent 的输入组合成一个下游的输入对象)。

### 关键点

- **点击拖动** 从输出端口到输入端口以创建连接
- **可选变换器** — 在数据通过时重塑数据的内联表达式
- **扇出** — 一个输出到多个下游输入(并行分支)
- **扇入** — 多个上游输出到一个下游输入(组合对象)
- **切换开/关** — 禁用连接而不删除它(对分阶段推出有用)
- **标记** — 为复杂 pipeline 中的清晰命名连接
- **删除** — 点击连接 → Delete 键

### 连接两个 agent

:::steps
1. **找到输出端口** — 源 agent 右边缘的小圆圈
2. **点击拖动** 到输入端口 — 目标左边缘的小圆圈
3. **放到输入端口上** — 线条绘制;连接已提交
4. **可选地添加变换器** — 右键单击连接 → Add transformer;编写小表达式以重塑数据
5. **通过运行 pipeline 进行测试** — 在运行期间点击任何连接以检查通过它的数据
:::

:::tip
在任何具有超过 3-4 个 agent 的 pipeline 中自由使用连接标签和变换器。标签使拓扑自我记录;变换器让你在 pipeline 之间保持 agent 可重用(一个 agent 不必知道不同 pipeline 上游可能产生什么格式 — 变换器会适配它)。
:::
  `,

  "pipeline-execution": `
## Pipeline 执行

运行 pipeline 将触发器载荷分派到第一个 agent(或多个 agent,如果有多个开始节点),每个下游 agent 在其输入可用时运行。画布实时显示执行 — agent 在运行时发光,连接随着数据流动而动画,条件节点显示采取了哪个分支。

引擎自动处理并行性:如果两个 agent 之间没有依赖关系,它们并行运行。如果 agent 依赖于来自多个上游 agent 的输出,它会等待所有完成。总挂钟时间由通过图的关键路径决定,而不是所有 agent 持续时间的总和。

### 关键点

- **实时画布动画** — 查看哪些 agent 正在运行、哪些连接正在流动、哪些条件分支被采取
- **自动并行性** — 独立 agent 并发运行;依赖 agent 等待先决条件
- **关键路径决定挂钟时间** — pipeline 持续时间 = 最长依赖链,而不是 agent 总和
- **首次失败停止** — 默认情况下;如果你想要容错执行,可按 pipeline 配置
- **从任何步骤重新运行** — 修复后接续,而不是重新运行成功的上游阶段

### 工作原理

:::diagram
[Trigger] --> [Agent A] --> [Conditional] --> [Agent B or Agent C] --> [Agent D] --> [Output]
:::

点击 \`Run\`(或等待触发器自动触发)。引擎从画布拓扑构建执行计划,分派开始节点,并按拓扑顺序处理图。每个 agent 完成时,下游 agent 变得有资格并自动分派。失败会在失败步骤暂停 pipeline,错误在检查器中可见;修复底层问题并点击 \`Retry Step\` 以恢复。

:::tip
关键路径上最慢的 agent 决定 pipeline 的持续时间。如果你的 pipeline 感觉慢,运行一次,查看追踪中的每 agent 持续时间,识别最长路径,并优化该路径上具有最高持续时间的任何 agent。如果你的关键路径慢,并行分支也无济于事。
:::
  `,

  "conditional-routing": `
## 条件路由

条件路由节点让 pipeline 根据它正在处理的数据进行分支。在画布上放置条件节点,定义一个或多个规则("如果金额 > 1000"、"如果邮件包含 'urgent'"、"如果分类器输出 = 'support'"),并将每个分支连接到不同的下游路径。在运行时,条件评估并路由到匹配的分支 — 只有该分支运行。

规则基于表达式:针对上游 agent 输出评估的比较和逻辑运算符的小 DSL。无需代码;表达式编辑器有上游输出形状的自动补全,所以你在输入时发现可用字段。

:::feature
**基于表达式的路由**
条件规则作为表达式针对上游输出评估。比较字段,使用 AND/OR 组合,在没有匹配时下降到默认分支。无需代码,但在需要时具有完整的表达能力。
:::

### 关键点

- **多个分支** — 一个条件节点,N 个规则定义的分支,加上默认后备
- **默认分支是强制的** — 保证数据永远不会因不匹配的条件而卡住
- **表达式 DSL** — 比较(\`>\`、\`<\`、\`==\`、\`contains\`、\`matches\`)、布尔运算符(\`and\`、\`or\`、\`not\`)
- **上游形状的自动补全** — 表达式编辑器知道上游 agent 的输出 schema
- **追踪中的实时评估** — 查看每次 pipeline 运行中采取了哪个分支

### 工作原理

在 agent 之间放置 Conditional 节点。在规则编辑器中配置每个分支的规则;默认分支不需要规则(它是后备)。在运行时,引擎按顺序评估规则;第一个匹配获胜;如果没有规则匹配,默认分支运行。运行的分支将上游输出视为输入;其他分支在此次运行中保持空闲。

:::warning
始终定义默认分支。没有它,不匹配的输入会卡在 pipeline 中并产生一个挂起的运行 — 调试起来很烦人。如果你真的想让不匹配的输入大声失败,默认分支可以简单地路由到终端"日志和停止" agent,但分支需要存在。
:::
  `,

  "team-members-and-roles": `
## 团队成员和角色

Pipeline 中的每个 agent 都可以携带一个角色标签 — "Researcher"、"Writer"、"Editor"、"Classifier" — 描述其在 pipeline 中的功能。角色纯粹是组织性的;引擎不强制或使用它们。它们的价值是人类:当你(或其他人)一个月后打开画布时,角色标签使 pipeline 自我记录。

除了标签,角色对于 agent 替换也很有用。如果你有多个 agent 可以填充 "Editor" 角色(具有不同的 prompt 风格或专长),角色标签让你在改变主意时清楚地知道要交换哪个槽。Team Canvas 支持在角色上拖放替换:在现有角色上放置不同的 agent,画布会询问是否替换,保留连接。

### 关键点

- **自由文本角色标签** — 任何人类可读的内容;常见的获得自动补全建议
- **画布可见** — 角色标签出现在每个 agent 节点上方,这样团队结构一目了然
- **按角色拖动替换** — 在角色槽上放置新 agent 以替换,保留连接
- **按角色过滤库** — 当你有许多类似的 agent 时,按角色过滤库以快速找到候选
- **Pipeline 模板使用角色** — 模板定义要填充的角色,你带来适合每个角色的 agent

### 工作原理

右键单击画布上的任何 agent → Set role。标签出现在 agent 节点上方。角色与 agent 引用一起存在于 pipeline 定义中;它们不修改 agent 本身。Pipeline 模板预定义角色;实例化模板会提示你为每个角色选择一个 agent。

:::tip
按职责命名角色,而不是按当前 agent。"Editor" 比 "Claude Sonnet Editor" 好;角色描述比当前填充它的特定 agent 持续更长时间。如果你从 Claude 切换到 GPT 用于该角色,角色标签仍然准确。
:::
  `,

  "pipeline-run-history": `
## Pipeline 运行历史

Pipeline 运行在与单个 agent 运行相同的存储中是一等执行。Pipeline → Run History 选项卡显示每次运行及其触发器、输入、状态、总持续时间、总成本和每 agent 细分。点击任何运行以展开完整追踪:每 agent 追踪、条件决策、变换器输出、最终结果。

运行历史无限期保留(受 Settings → Data 中的保留设置约束),并支持与每 agent 活动视图相同的过滤和搜索。每次运行都是不可变的 — 一旦捕获,追踪被冻结,对于事后审计很有用。

### 关键点

- **完整捕获** — 输入、每 agent 追踪(prompt、工具调用、响应)、条件决策、变换器输出、最终结果
- **pipeline 追踪中的每 agent 状态** — 成功 / 失败 / 跳过 / 待处理
- **总计 + 每 agent 计时** — 查看关键路径并识别瓶颈
- **总计 + 每 agent 成本** — pipeline 成本 = 每 agent 成本之和
- **可搜索和可过滤** — 按日期、触发器、状态、成本、持续时间、agent
- **两次运行比较** — 选择两次运行以比较每 agent 输出(对"变了什么?"有用)

### 工作原理

Pipeline 运行使用与单 agent 运行相同的执行存储,但带有一个额外的 pipeline 级包装器,链接到所有子 agent 执行。历史视图查询此存储,加入 agent 执行记录以获得每 agent 细分,并渲染追踪树。

:::tip
在有意义的 pipeline 更改(新的条件规则、交换的 agent、成员 agent 上的 prompt 修订)之后,从历史中选择一个"之前"运行和一个来自新运行的"之后"运行,然后使用 Compare 准确查看不同之处。Pipeline 级别的差异通常揭示了你在隔离查看任何单个 agent 时会错过的影响。
:::
  `,

  "pipeline-templates": `
## Pipeline 模板

Pipeline 模板是你可以作为起点采用的预构建 pipeline 形状。模板定义拓扑 — 存在什么角色、什么条件分支、什么变换器 — 但不将特定的 agent 绑定到每个角色。当你实例化模板时,画布以拓扑就位打开,并提示你从自己的 agent 库填充每个角色。

模板涵盖常见形状:内容工作流(research → write → edit → publish)、支持分类(classify → route → respond → escalate)、数据处理(ingest → validate → transform → store)。模板库位于 Pipelines → New Pipeline → Browse Templates。

### 关键点

- **拓扑定义,角色灵活** — 模板知道形状;你带来 agent
- **预配置的条件规则和变换器** — 常见情况路由逻辑已烘焙
- **实例化后可自定义** — 一旦实例化,画布是你的,可以修改
- **最佳实践模式** — 模板默认带有错误处理和后备分支
- **增长的库** — 根据用户需求添加新模板;你也可以将自己的 pipeline 保存为模板以供重用

### 工作原理

模板是带有角色槽而不是 agent 引用的画布定义。实例化创建新 pipeline,复制模板的画布,并要求你从 agent 库填充每个角色。一旦填充,pipeline 完全可编辑 — 它不与模板链接,所以模板的更新不会传播(对 pipeline 的编辑不会影响模板)。

:::tip
即使没有完全适合的模板,选择最接近的并修改它通常比从头构建更快。模板预先解决了编排形状(条件位置、变换器位置、扇出/扇入拓扑);剩下的工作是 agent 选择和 prompt 调整,这是你想专注的工作。
:::
  `,

  "team-assignments": `
## 团队任务分配

Pipeline 需要你手动配置每一个步骤。任务分配则将这个逻辑倒转过来:你给团队一个**目标**,用自然语言表达,团队自己规划出具体步骤。它将目标拆解为任务清单,为每个步骤匹配最合适的 agent 并并行执行 — 只有在某个步骤失败或需要决策时才会停下来询问你。

这就像画流程图和向项目经理简要汇报之间的区别。Pipeline 让你设计流程;任务分配让你说明结果,由团队自行组织。

### 关键点

- **目标优先** — 描述你想要什么,团队将其拆解为有序步骤
- **智能匹配** — 每个步骤被路由到最合适的 agent(可以手动指定 agent、使用快速本地匹配,或让模型决定)
- **自动拆解** — 一键将目标转化为可编辑的步骤列表,运行前还可以调整
- **并行执行** — 独立步骤同时运行;有依赖关系的步骤依次等待
- **失败时人工审核** — 某个步骤失败会暂停该任务分配并弹出编辑 / 重新分配 / 跳过选项,同时在标题栏显示通知
- **可复用模板** — 将目标和步骤布局保存为模板,之后一键创建新的任务分配
- **对话调度** — 让 Athena "让研究团队处理这个",她会为你设置好等待审批

### 工作原理

打开某个团队的画布,点击左下角的**任务分配**徽章。点击**新建**,输入目标,然后自行填写步骤,或点击**自动拆解**让助手为你提议步骤。选择 agent 与步骤的匹配方式,设置同时运行的步骤数量,再点击**创建并启动**。任务清单会实时更新;如果某个步骤失败,可在行内解决。将需要反复运行的任务保存为模板。

:::tip
当你知道想要的结果但不确定具体步骤时,使用任务分配。当你需要精确、可重复地控制每一个连接时,使用 pipeline。模板弥合了两者之间的差距 — 保存的任务分配可作为一键起点。
:::
  `,

  "team-memory-and-goals": `
## 团队 Memory 与目标

团队不只是一组 agent — 而是一群**共同记忆**、共同朝着目标努力的 agent。支撑这一切的有两件事:共享团队 memory 和目标。

### 共享团队 memory

团队在工作过程中会记录决策与约束 — "我们统一了这种格式"、"这个账户不在范围内"、"审稿人否定了方案 X"。这些记录构成**团队 memory**,最重要内容的精简摘要会在每次运行时注入每位成员的上下文。

效果是:团队不断收敛而不是不停重复。一个 agent 不会重新发现队友已经做出的决策 — 而是直接继承它。你可以在画布的团队 Memory 面板中查看和管理这些 memory。

### 目标 — 指引方向而不陷入微观管理

将团队与一个**目标**关联,你就不再需要盯着每一次运行了。随着团队工作推进,目标自动跟踪进度,应用只将真正需要人介入的事项浮现出来 — 停滞的目标、临近的截止日期、等待审核的步骤。其余的事情自行运转。

这就是"定好方向、保持高层视角"的工作循环:你定义结果和边界,团队负责执行,有需要时主动举手。

:::tip
把团队 memory 想象成团队的机构知识,把目标想象成团队的北极星。Memory 保证团队每次运行保持一致;目标保证团队朝着有价值的方向前进。
:::

### 关键点

- **共享 memory** — 团队记录的决策与约束会注入每位成员的下一次运行
- **收敛** — 成员在彼此的结论基础上继续推进,而不是重新推导
- **目标关联** — 将团队与目标绑定以跟踪进度和截止日期
- **浮现而不是埋没** — 注意力队列只展示需要你介入的内容(停滞、逾期、等待审核)
- **主动管理** — 在画布面板中审查、编辑或删除团队 memory
  `,

  "debugging-pipeline-issues": `
## 调试 pipeline 问题

当 pipeline 运行失败时,画布用红色指示器标记失败的 agent,运行在该步骤暂停。从历史中打开失败的运行(或点击实时画布上的指示器),调试面板显示 agent 的输入、错误、失败前的追踪以及 agent 在失败前产生的任何部分输出。从同一面板,你可以重试失败的步骤或从头重新运行整个 pipeline。

最常见的 pipeline 失败是数据形状不匹配 — 上游 agent 产生的输出格式与下游 agent 期望的格式略有不同。连接检查器(点击任何连接)显示最近运行中通过它的数据,通常足以发现不匹配。

### 关键点

- **失败步骤突出显示** — 画布上的红色指示器,调试面板中的完整错误
- **连接检查器** — 点击任何连接以查看通过它的实时或上次运行的数据
- **从失败步骤重试** — 修复问题并恢复;成功的上游阶段不会重新运行
- **逐步重放** — 用相同的输入重新运行任何过去的 pipeline 执行以确定性地重现失败
- **连接验证** — 画布可以预先检查上游和下游 agent 是否有兼容的输入/输出形状(在运行时间之前捕获不匹配)

### 工作原理

Pipeline 引擎在 agent 运行错误时发出结构化失败事件。调试面板订阅这些事件并渲染相关追踪 + 检查器。从步骤重试由引擎支持:它使用相同的上游上下文重新分派失败的 agent,保留 pipeline 运行的其余部分。

:::tip
大多数 pipeline 失败是连接问题,而不是 agent 问题。当事情损坏时,首先检查馈送失败 agent 的连接 — 它实际接收到什么形状?这通常更多是"数据是错误的"而不是"agent 是错误的";连接检查器在一分钟内告诉你是哪种情况。
:::
  `,
};
