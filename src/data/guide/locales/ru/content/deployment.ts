export const content: Record<string, string> = {
  "github-actions-integration": `
## Интеграция с GitHub Actions

Агенты могут запускать GitHub Actions workflows через инструмент GitHub на вкладке Connectors, а GitHub Actions могут запускать агентов через стандартный webhook-триггер. Эти два паттерна хорошо комбинируются: событие GitHub (открыт PR, push в main, помечен релиз) срабатывает webhook, запускающий агента Personas, агент делает своё дело, и (при необходимости) агент запускает workflow как часть вывода.

Коннектор GitHub поставляется в каталоге (Connections → Catalog → Developer Tools → GitHub). Аутентификация — OAuth или fine-grained PAT — OAuth предпочтительнее, когда агенту нужен только доступ для чтения; PATs хорошо работают для операций записи вроде запуска workflows.

### Ключевые моменты

- **GitHub → Personas через входящий webhook** — стандартный webhook-триггер; настройте GitHub на POST к URL агента
- **Personas → GitHub через инструмент GitHub** — агент может запускать workflows, комментировать PRs, открывать issues, всё, что предоставляет GitHub API
- **Scoped аутентификация** — OAuth для агентов в основном чтения, fine-grained PAT для операций записи; минимальные scopes на агента
- **Живая синхронизация статуса** — трейсы агента показывают запрос workflow_dispatch и ответ GitHub; агент может ждать завершения workflow при необходимости

### Как это работает

:::diagram
[GitHub event] --> [Inbound webhook] --> [Agent decides] --> [GitHub tool dispatches workflow] --> [Workflow result back into trace]
:::

Инструмент GitHub оборачивает REST/GraphQL API GitHub и предоставляет агенту высокоуровневые действия: «запустить workflow», «комментировать PR», «открыть issue», «слить PR» и т. д. Промпт агента называет действие на основе триггера; инструмент обрабатывает аутентификацию, построение payload и обработку ответа.

:::warning
Используйте fine-grained PATs вместо классических PATs всегда, когда ваш тариф GitHub их поддерживает. Классические PATs дают широкие разрешения на уровне организации; fine-grained PATs ограничены конкретными репозиториями и конкретными scopes разрешений, что сильно сужает blast radius, если токен когда-либо утечёт.
:::

:::tip
Начните с workflow низкого риска как цели — например, «уведомить Slack», который просто публикует сообщение. После того как передача агент → GitHub Actions доказана, переходите к целям более высокого риска (деплой, выпуск релиза и т. д.).
:::
  `,

  "gitlab-ci-cd-integration": `
## Интеграция с GitLab CI/CD

Personas интегрируется с GitLab двумя способами: прямой плагин GitLab, дающий агентам API-уровневый доступ (статус пайплайна, комментарии MR, управление issues), и экспорт GitLab CI YAML, запускающий агентов Personas как шаги внутри ваших существующих пайплайнов. Оба поставляются; выбирайте подходящий под форму workflow вашей команды.

Плагин (Plugins → GitLab) обрабатывает интеграцию на стороне API: установите, аутентифицируйтесь, и агенты получают поверхность инструмента \`gitlab\` с высокоуровневыми действиями (запуск пайплайна, комментирование MR, управление issues). Экспорт CI YAML идёт в обратную сторону — ваши агенты становятся шагами в GitLab CI пайплайнах, выполняются GitLab runners, с результатами, передаваемыми следующим шагам.

### Ключевые моменты

- **Плагин GitLab** — интеграция на уровне API; агент использует GitLab как инструмент со вкладки Connectors
- **Экспорт CI YAML** — агент становится шагом в вашем GitLab пайплайне; запускается на ваших GitLab runners
- **Двунаправленно** — события GitLab могут запускать агентов (webhook), а агенты могут запускать GitLab пайплайны (плагин)
- **Scopes токенов** — используйте project access tokens или group access tokens, ограниченные минимально необходимыми разрешениями
- **События пайплайна как триггеры** — \`Pipeline succeeded\`, \`Pipeline failed\`, \`MR merged\` — всё это потребляется через webhook-триггер

### Как это работает

Плагин использует GitLab API токены, хранящиеся в хранилище учётных данных. Когда агент вызывает действие инструмента GitLab, движок отправляет API-вызов, захватывает ответ и подаёт обратно как результат инструмента для следующего хода модели.

Для CI-экспорта: откройте вкладку Settings агента → Export → GitLab CI YAML. Мастер генерирует определение задачи, оборачивающее агента в CI-запускаемую форму (обычно Docker-образ с CLI Personas плюс ссылка на агента). Закоммитьте сгенерированный YAML в \`.gitlab-ci.yml\` вашего репозитория; агент работает как часть пайплайна рядом с любыми другими CI-задачами.

:::warning
Экспортированный CI YAML ссылается на переменные учётных данных для вещей вроде ключей провайдеров ИИ. Определите их как **masked, protected** GitLab CI/CD-переменные в настройках проекта — никогда не зашивайте секреты в сам файл YAML, поскольку YAML пайплайна живёт в репозитории и виден всем с доступом на чтение.
:::

:::tip
Плагин — более легковесная опция для большинства команд. Экспорт CI YAML наиболее полезен, когда агенту так или иначе нужно работать внутри GitLab runner (сетевая изоляция, ресурсы внутренней сети, инфраструктура, обязательная по compliance) — иначе плагин позволяет держать агента в Personas, где его наблюдаемость и отладка богаче всего.
:::
  `,

  "n8n-workflow-integration": `
## Интеграция с рабочими процессами n8n

n8n — это популярный open-source инструмент автоматизации workflow, и Personas интегрируется с ним двунаправленно. Можно импортировать существующие n8n workflows в Personas как шаблоны (Templates → n8n Import) — мастер импорта парсит JSON workflow и мапит узлы n8n в эквивалентных агентов, коннекторов и триггеров Personas. Можно также вызывать агентов Personas *из* n8n, используя HTTP/webhook узлы для вызова URL входящего webhook агента.

Импорт n8n односторонний и одноразовый: он приносит *форму* workflow в Personas, но не держит оригинал n8n синхронизированным. После импорта импортированный конвейер ваш для независимого редактирования.

### Ключевые моменты

- **n8n → Personas импорт** — Templates → n8n Import; парсит JSON workflow, мапит узлы в эквиваленты Personas
- **Personas → n8n триггер** — HTTP/webhook узлы n8n могут делать POST на URL webhook-триггера агента
- **n8n → Personas триггер** — n8n может вызвать webhook агента Personas как часть n8n workflow; ответ агента (настраиваемый) течёт обратно в n8n
- **Не синхронизируются** — импортированные конвейеры расходятся с n8n-источником; рассматривайте импорт как разовую отправную точку
- **Покрытие мапинга узлов** — импортёр обрабатывает распространённые узлы (HTTP, function, IF, switch); экзотические / коммьюнити-узлы могут импортироваться как заглушки для ручного завершения

### Как это работает

Мастер импорта читает JSON workflow n8n (экспорт из n8n → «Download» на workflow), мапит каждый узел в ближайший эквивалент Personas (HTTP-узлы → инструменты, function-узлы → агенты, IF/switch → условная маршрутизация и т. д.) и подготавливает результат как конвейер, который вы предварительно просматриваете перед принятием. Мапинг — best-effort: всё, что импортёр не может уверенно сопоставить, становится заглушкой с пометкой для вашего заполнения.

В обратном направлении URL webhook агента Personas — это просто URL: любой HTTP-узел n8n может вызвать его. Передавайте вход как тело запроса; агент обрабатывает и (опционально) отвечает синхронно с выводом.

:::tip
n8n преуспевает в «перемещении данных между сервисами»; Personas преуспевает в «мышлении» — анализе, решениях, написании. Самые сильные комбинированные workflows используют n8n для оркестрации плюс агентов Personas для точек принятия решений на базе ИИ, а не пытаются делать всё одного в другом.
:::
  `,

};
