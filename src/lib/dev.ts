/**
 * Development mode flag.
 * When true, the app uses mock data instead of real Supabase auth and orchestrator API calls.
 * Set to false (or remove) for production.
 */
export const DEVELOPMENT = true;
